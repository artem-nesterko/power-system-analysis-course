# Лекція 17: Аналіз надійності N - 1 та модифікація розріджених систем за формулою Шермана-Моррісона (PTDF/LODF)

## 1. Теоретична частина

### Критерій надійності N - 1
Фундаментальним правилом проектування та експлуатації енергосистем є критерій **N - 1**. Він означає, що система повинна залишатися стійкою і продовжувати постачати електроенергію всім споживачам у разі аварійного відключення будь-якого **одного** елемента (генератора, трансформатора або ЛЕП).

Для диспетчера це означає необхідність постійно, кожні кілька хвилин, прораховувати тисячі сценаріїв: "А що буде, якщо відключиться лінія А? А якщо лінія Б?". Якщо в мережі 10 000 ліній, треба 10 000 разів розв'язати систему $Y \cdot U = I$. 
Навіть з розрідженими методами (CSR, Tinney-2) повна перефакторизація матриці 10 000 разів поспіль забирає занадто багато часу. Чи можна перерахувати режим без перефакторизації?

### Формула Шермана-Моррісона (Sherman-Morrison Formula)
Математика дає блискучу відповідь: так! Якщо матриця $A$ зазнає зміни лише **рангу 1** (наприклад, додається або видаляється один зв'язок між вузлами $i$ та $j$), то обернену матрицю $A_{new}^{-1}$ можна знайти з попередньої оберненої матриці $A^{-1}$ за допомогою формули Шермана-Моррісона:

$$(A + u \cdot v^T)^{-1} = A^{-1} - \frac{A^{-1} u \cdot v^T A^{-1}}{1 + v^T A^{-1} u}$$

де $u$ та $v$ — вектори-стовпці.

В електроенергетиці відключення лінії $k$ між вузлами $i$ та $j$ з провідністю $y_k$ (де $y_k = 1/x_k$ для DC Power Flow) означає віднімання від $Y_{bus}$ матриці рангу 1:
$\Delta Y = -y_k \cdot (e_i - e_j)(e_i - e_j)^T$
де $e_i$ — вектор, у якого всі нулі, окрім 1 на $i$-тій позиції.

Якщо покласти $u = e_i - e_j$ та $v = -y_k (e_i - e_j)$, ми отримаємо оновлену матрицю опорів $Z_{new} = Y_{new}^{-1}$ без жодної факторизації, лише через декілька операцій множення вектора на вектор!

> **Нотатка від професора:**
> На практиці ми рідко обчислюємо саму обернену матрицю $Z$. Формула Шермана-Моррісона лежить в основі розрахунку лінійних коефіцієнтів чутливості (PTDF — Power Transfer Distribution Factors та LODF — Line Outage Distribution Factors). Замість перерахунку всієї матриці, диспетчерська програма обчислює лише те, **як перерозподілиться струм відключеної лінії по інших лініях** (це і є LODF). Це робить аналіз $N-1$ практично миттєвим.

> **Глибока інтуїція: Електромагнітна інерція мережі (LODF)**
> Уявіть мережу як систему водопровідних труб. Якщо перекрити одну трубу (аварія ЛЕП), вода (струм) не зникає, вона миттєво перерозподіляється по паралельних шляхах. Звідки мережа "знає", куди текти? Це визначається виключно топологією (спротивом паралельних шляхів).
> Вектор $Z \cdot u$ (або розв'язок $Y \cdot z = u$) у формулі Шермана-Моррісона — це не просто алгебра. Це математичне моделювання "пробної інжекції": ми впорскуємо 1 ампер у вузол $i$ і витягуємо 1 ампер з вузла $j$. Відгук мережі на цю пробну інжекцію дозволяє точно передбачити, куди хлине справжня потужність після відключення лінії. Тобто ми аналізуємо аварію ще до її настання, спостерігаючи за топологічною "пружністю" графа!

### Приклад ручного розрахунку
Маємо початкову матрицю $Y$ (редуковану) для мережі 3 вузлів (вузол 0 — баланс, вузли 1 і 2 — навантаження). ЛЕП 0-1 ($y=10$), 0-2 ($y=10$), 1-2 ($y=5$).
$Y = \begin{bmatrix} 15 & -5 \\ -5 & 15 \end{bmatrix}$
Обернена матриця (опорів):
$Z = Y^{-1} = \begin{bmatrix} 0.075 & 0.025 \\ 0.025 & 0.075 \end{bmatrix}$

Відключаємо лінію 1-2 ($y=5$). 
$u = e_1 - e_2 = \begin{bmatrix} 1 \\ -1 \end{bmatrix}$
$v = -y \cdot u = \begin{bmatrix} -5 \\ 5 \end{bmatrix}$

1. Знайдемо вектор $w = Z \cdot u$:
$w = \begin{bmatrix} 0.075 - 0.025 \\ 0.025 - 0.075 \end{bmatrix} = \begin{bmatrix} 0.05 \\ -0.05 \end{bmatrix}$

2. Знайдемо знаменник $D = 1 + v^T w$:
$v^T w = (-5)(0.05) + (5)(-0.05) = -0.25 - 0.25 = -0.5$
$D = 1 - 0.5 = 0.5$

3. Оновлення матриці $Z_{new} = Z - \frac{w \cdot (v^T Z)}{D}$:
$v^T Z = [-5, 5] \cdot \begin{bmatrix} 0.075 & 0.025 \\ 0.025 & 0.075 \end{bmatrix} = [-0.25, 0.25]$
Матриця поправок: $\frac{1}{0.5} \begin{bmatrix} 0.05 \\ -0.05 \end{bmatrix} [-0.25, 0.25] = 2 \begin{bmatrix} -0.0125 & 0.0125 \\ 0.0125 & -0.0125 \end{bmatrix} = \begin{bmatrix} -0.025 & 0.025 \\ 0.025 & -0.025 \end{bmatrix}$

4. Новий стан:
$Z_{new} = \begin{bmatrix} 0.075 & 0.025 \\ 0.025 & 0.075 \end{bmatrix} - \begin{bmatrix} -0.025 & 0.025 \\ 0.025 & -0.025 \end{bmatrix} = \begin{bmatrix} 0.1 & 0 \\ 0 & 0.1 \end{bmatrix}$
Збігається з фізикою! Після відключення лінії 1-2 вузли 1 і 2 живляться лише по своїх радіальних лініях від балансуючого вузла з опорами $x=1/10=0.1$. Взаємний опір дорівнює нулю.

## 2. Приклад розрахунку на C#
Нижче наведено код на `MathNet.Numerics`, що демонструє блискавичну зміну матриці Zbus за формулою Шермана-Моррісона.

```csharp
// Program.cs
using System;
using MathNet.Numerics.LinearAlgebra;

public class Program
{
    public static void Main(string[] args)
    {
        Console.WriteLine("--- Аналіз надійності N-1 (Формула Шермана-Моррісона) ---");
        
        // Початкова матриця Ybus (редукована)
        var Y = Matrix<double>.Build.DenseOfArray(new double[,] {
            { 15.0, -5.0 },
            { -5.0, 15.0 }
        });
        
        // Початкова матриця Zbus
        var Z = Y.Inverse();
        
        Console.WriteLine("Початкова матриця Zbus:");
        Console.WriteLine(Z.ToString("F4"));
        
        // Параметри лінії, що відключається: вузли 1 і 2 (індекси 0 і 1), y = 5.0
        double y_line = 5.0;
        int i = 0;
        int j = 1;
        
        // Вектори для формули Шермана-Моррісона
        var u = Vector<double>.Build.Dense(2);
        u[i] = 1.0;
        u[j] = -1.0;
        
        var v = -y_line * u;
        
        // Крок 1: Знаменник D = 1 + v^T * Z * u
        var Z_u = Z * u;
        double denom = 1.0 + v.DotProduct(Z_u);
        
        // Крок 2: Чисельник = (Z * u) * (v^T * Z)
        var vT_Z = v * Z;
        var Delta_Z = Z_u.OuterProduct(vT_Z) / denom;
        
        // Крок 3: Оновлена матриця
        var Z_new = Z - Delta_Z;
        
        Console.WriteLine("Нова матриця Zbus (Шерман-Моррісон):");
        Console.WriteLine(Z_new.ToString("F4"));
        
        // Перевірка
        var Y_new = Matrix<double>.Build.DenseOfArray(new double[,] {
            { 10.0,  0.0 },
            {  0.0, 10.0 }
        });
        Console.WriteLine("Нова матриця Zbus (Пряме обернення Y_new):");
        Console.WriteLine(Y_new.Inverse().ToString("F4"));
    }
}
```

## 3. Перевірка правильності обрахунку
1.  Скомпілюйте і запустіть програму. Результат за формулою Шермана-Моррісона має з точністю до $10^{-12}$ збігатися з прямим оберненням нової матриці `Y_new`.
2.  Знаменник `denom` не повинен дорівнювати нулю. Якщо він нуль — це фізично означає, що відключення цієї лінії розбило мережу на два ізольовані острови (islanding), матриця $Y_{new}$ стає виродженою (сингулярною) і сталої напруги більше не існує.

## 4. Завдання для самостійної роботи
Перевірте відключення лінії для матриці свого варіанту. Використайте програму на C#, щоб знайти нову $Z_{new}$. Порівняйте з прямим розрахунком.

В усіх варіантах відключаємо лінію між вузлами 1 і 2 (індекси 0 і 1 в C#). Провідність лінії $y=4.0$.

| Вар | `Ybus` (початкова, по рядках) |
|---|---|
| 1 | `[[ 12.0, -4.0 ], [ -4.0, 14.0 ]]` |
| 2 | `[[ 15.0, -4.0 ], [ -4.0, 10.0 ]]` |
| 3 | `[[  9.0, -4.0 ], [ -4.0, 11.0 ]]` |
| 4 | `[[ 16.0, -4.0 ], [ -4.0, 12.0 ]]` |
| 5 | `[[ 11.0, -4.0 ], [ -4.0, 13.0 ]]` |
| 6 | `[[ 13.0, -4.0 ], [ -4.0,  9.0 ]]` |
| 7 | `[[ 18.0, -4.0 ], [ -4.0, 10.0 ]]` |
| 8 | `[[ 14.0, -4.0 ], [ -4.0, 15.0 ]]` |
| 9 | `[[ 10.0, -4.0 ], [ -4.0,  8.0 ]]` |
| 10| `[[ 17.0, -4.0 ], [ -4.0, 14.0 ]]` |
