# Лекція 7: Методи оптимізації другого порядку (метод Ньютона)

## 1. Теоретична частина

### Переваги методів другого порядку
У попередній лекції ми розглядали градієнтний спуск, який для руху використовує лише інформацію про "напрямок схилу" (першу похідну). Це надійний, але часто повільний метод.

Методи другого порядку, головним з яких є **метод Ньютона**, використовують додаткову інформацію — про **"кривизну"** поверхні (другу похідну). Це дозволяє їм "бачити" загальну форму функції значно краще і знаходити мінімум набагато швидше.

> **Нотатка від професора: Градієнтний спуск vs. Метод Ньютона**
>
> *   **Градієнтний спуск** — це як спускатися з гори в тумані. Ви робите невеликий крок у напрямку найкрутішого схилу, зупиняєтесь, оцінюєте новий напрямок і робите наступний крок. Це працює, але якщо ви у витягнутому яру, ви будете довго "зигзагувати" від одного схилу до іншого.
> *   **Метод Ньютона** — це як використати дрон, щоб оглянути рельєф навколо. Ви будуєте локальну 3D-модель поверхні (квадратичну апроксимацію), математично знаходите найнижчу точку на цій моделі і робите один великий "стрибок" прямо туди. Для багатьох функцій це значно ефективніше.

### Матриця Гессе (Гессіан)
Ключовим поняттям для методів другого порядку є **матриця Гессе `H`**.
- **Що це?** Це матриця других часткових похідних цільової функції `F`.
- **Що вона описує?** Вона повністю описує **кривизну** функції в заданій точці. Завдяки їй ми знаємо, чи схожа поверхня в цьому місці на "чашу", "сідло" чи "купол".

Для функції двох змінних `F(x, y)` гессіан має вигляд:
```
      | ∂²F/∂x²   ∂²F/∂x∂y |
H  =  |--------------------|
      | ∂²F/∂y∂x   ∂²F/∂y² |
```
Гессіан завжди є симетричною матрицею.

### Метод Ньютона для оптимізації
Ідея методу полягає в тому, щоб на кожній ітерації апроксимувати нашу складну функцію `F(X)` простішою моделлю (параболоїдом), а потім знайти точний мінімум цієї моделі за один аналітичний крок. Цей крок `ΔX` (напрямок Ньютона) знаходиться шляхом розв'язання лінійної системи:

**[H(X⁽ᵏ⁾)] * [ΔX] = -∇F(X⁽ᵏ⁾)**

де:
- `X⁽ᵏ⁾` - поточна точка.
- `∇F(X⁽ᵏ⁾)` - градієнт (вектор перших похідних) в цій точці.
- `H(X⁽ᵏ⁾)` - матриця Гессе (матриця других похідних) в цій точці.
- `ΔX` - шуканий крок до мінімуму.

Нова точка знаходиться як: **X⁽ᵏ⁺¹⁾ = X⁽ᵏ⁾ + ΔX**

- **Переваги:** Дуже висока (квадратична) швидкість збіжності. Для простих квадратичних функцій він знаходить мінімум за одну ітерацію.
- **Недоліки:**
    1.  **Обчислювальна складність:** Потрібно розраховувати не тільки вектор перших похідних, а й цілу матрицю других похідних.
    2.  **Розв'язання СЛАР:** На кожній ітерації потрібно розв'язувати систему лінійних рівнянь, що для великих задач є затратним.
    3.  **Нестабільність:** Метод може бути нестабільним, якщо гессіан не є додатно визначеним (тобто якщо функція в цій точці не схожа на "чашу").

## 2. Приклад розрахунку на C# (одна ітерація)
Цей приклад демонструє один крок методу Ньютона для знаходження мінімуму складної функції Розенброка `F(x, y) = 100(y - x²)² + (1 - x)²`. Ця функція має мінімум в точці `(1, 1)` і є класичним тестом для алгоритмів оптимізації.

```csharp
// Program.cs
using System;
using MathNet.Numerics.LinearAlgebra;

public class Program
{
    // Цільова функція (Розенброка)
    public static double Function(Vector<double> X)
    {
        double x = X[0];
        double y = X[1];
        return 100 * Math.Pow(y - x * x, 2) + Math.Pow(1 - x, 2);
    }

    // Градієнт цільової функції (вектор перших похідних)
    public static Vector<double> Gradient(Vector<double> X)
    {
        double x = X[0];
        double y = X[1];
        double df_dx = -400 * x * (y - x * x) - 2 * (1 - x);
        double df_dy = 200 * (y - x * x);
        return Vector<double>.Build.Dense(new double[] { df_dx, df_dy });
    }

    // Матриця Гессе (матриця других похідних)
    public static Matrix<double> Hessian(Vector<double> X)
    {
        double x = X[0];
        double y = X[1];
        double d2f_dx2 = 1200 * x * x - 400 * y + 2;
        double d2f_dxdy = -400 * x;
        double d2f_dy2 = 200;
        return Matrix<double>.Build.DenseOfArray(new double[,] {
            { d2f_dx2, d2f_dxdy },
            { d2f_dxdy, d2f_dy2 } // d2f_dydx = d2f_dxdy
        });
    }

    public static void Main(string[] args)
    {
        // --- Початкова точка ---
        Vector<double> X_current = Vector<double>.Build.Dense(new double[] { 0, 0 });
        Console.WriteLine($"Початкова точка: X(0) = ({X_current[0]}, {X_current[1]})");
        Console.WriteLine($"Значення функції в початковій точці: F(X(0)) = {Function(X_current)}");

        // --- Крок 1: Розраховуємо градієнт та Гессіан в поточній точці ---
        Vector<double> grad = Gradient(X_current);
        Matrix<double> H = Hessian(X_current);

        Console.WriteLine($"Градієнт в точці X(0): ∇F = {grad}");
        Console.WriteLine($"Гессіан в точці X(0):\n{H}");

        // --- Крок 2: Розв'язуємо систему H * ΔX = -grad для знаходження кроку Ньютона ΔX ---
        Vector<double> deltaX = H.Solve(-grad);
        Console.WriteLine($"Крок Ньютона: ΔX = {deltaX}");
        
        // --- Крок 3: Робимо крок оптимізації ---
        Vector<double> X_new = X_current + deltaX;

        Console.WriteLine($"\n--- Результат 1-ї ітерації ---");
        Console.WriteLine($"Нова точка: X(1) = {X_new}");
        Console.WriteLine($"Значення функції в новій точці: F(X(1)) = {Function(X_new)}");
    }
}
```

### 3. Перевірка правильності обрахунку
1.  **Перевірте аналітичні похідні:** Переконайтесь, що часткові похідні для градієнта та гессіана розраховані вірно. Це найпоширеніше джерело помилок.
2.  **Розв'язок СЛАР:** Перевірте, що система `H * ΔX = -grad` розв'язується коректно. Для початкової точки `(0,0)`, `H = [[2, 0], [0, 200]]` та `-grad = [2, 0]`. Розв'язок `ΔX = [1, 0]` є правильним.

## 4. Завдання для самостійної роботи
Виконайте **ручний розрахунок** першого кроку методу Ньютона для вашого варіанту початкової точки, використовуючи ту ж саму функцію Розенброка.

**Алгоритм:**
1.  Запишіть початкову точку `X(0)`.
2.  Обчисліть градієнт `∇F` в точці `X(0)`, підставивши ваші `x` та `y` у формули.
3.  Обчисліть гессіан `H` в точці `X(0)`.
4.  Розв'яжіть систему `H * ΔX = -∇F` щоб знайти `ΔX`.
5.  Обчисліть нову точку: `X(1) = X(0) + ΔX`.

**Дані для варіантів (Функція та ж сама):**

| Вар | `X(0)` |
|---|---|\n| 1 | `(0, 0)` |\n| 2 | `(1, 2)` |\n| 3 | `(-1, 2)` |\n| 4 | `(2, 2)` |\n| 5 | `(0.5, 0.5)` |\n| 6 | `(-0.5, 0)` |\n| 7 | `(1.5, 2)` |\n| 8 | `(-1, -1)` |\n| 9 | `(0, 1)` |\n| 10 | `(2, 3)` |