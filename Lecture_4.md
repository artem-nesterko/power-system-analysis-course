# Лекція 4: Метод блочної подвійної факторизації

## 1. Теоретична частина

### Навіщо потрібні ефективні розв'язувачі?
На кожній ітерації методу Ньютона ми розв'язуємо систему лінійних рівнянь (СЛАР): `[J] * [ΔX] = [ΔW]`. Для реальних енергосистем з тисячами вузлів матриця Якобі `[J]` стає гігантською. Крім того, вона є **розрідженою** - більшість її елементів є нулями, оскільки вузли з'єднані лише з найближчими "сусідами".

> **Нотатка: Факторизація vs. Пряме обернення**
>
> Намагатись знайти обернену матрицю `[J]⁻¹` для великої системи - це як розв'язувати гігантський Sudoku, намагаючись вгадати всі цифри одночасно. Це надзвичайно довго і може призвести до великих обчислювальних похибок.
>
> **Факторизація** (розкладання) - це значно розумніший підхід. Це схоже на логічний підхід до Sudoku, де ви послідовно заповнюєте комірки, спираючись на вже відомі значення. Ми розкладаємо складну матрицю `J` на добуток простіших (наприклад, трикутних) матриць, роботу з якими можна організувати набагато ефективніше.

### Метод блочної подвійної факторизації
Цей метод є узагальненням класичного методу Гауса, але для матриць, що складаються з менших матриць-блоків. Розглянемо СЛАР, представлену у блочному вигляді 2x2:
```
| Aₖₖ  Aₖₛ | | Xₖ |   | Yₖ |
|--------| |----| = |----|
| Aₛₖ  Aₛₛ | | Xₛ |   | Yₛ |
```
де `A...` - матриці-блоки, а `X...`, `Y...` - вектори-блоки.

**Алгоритм розв'язання:**

1.  **Прямий хід (Факторизація/Виключення):**
    -   `A*ₖₖ = A⁻¹ₖₖ` (обертаємо тільки перший, менший, діагональний блок).
    -   `A*ₖₛ = A*ₖₖ * Aₖₛ`
    -   `A*ₛₖ = Aₛₖ * A*ₖₖ`
    -   `A'ₛₛ = Aₛₛ - Aₛₖ * A*ₖₛ`

    **Що таке `A'ₛₛ`?** Це **доповнення Шура**. Якщо просто, то це оновлений блок `Aₛₛ`, з якого ми "виключили" весь вплив верхньої частини системи. Тепер він містить всю інформацію, необхідну для знаходження `Xₛ`.

2.  **Модифікація правої частини:**
    -   `Y'ₛ = Yₛ - A*ₛₖ * Yₖ` (так само коригуємо праву частину).

3.  **Зворотний хід (Підстановка):**
    -   `Xₛ = (A'ₛₛ)⁻¹ * Y'ₛ` (розв'язуємо меншу систему для `Xₛ`).
    -   `Xₖ = A*ₖₖ * Yₖ - A*ₖₛ * Xₛ` (знаходимо `Xₖ`, використовуючи вже відомий `Xₛ`).

Перевага методу в тому, що замість обернення однієї великої матриці ми обертаємо дві менші (`Aₖₖ` та `A'ₛₛ`), що значно швидше.

### Приклад ручного розрахунку
Виконаємо прямий хід для матриці 4x4, розбивши її на блоки 2x2.
`Aₖₖ = [[2, -1], [3, 2]]`, `Aₖₛ = [[3, 1], [15, 10]]`
`Aₛₖ = [[2, 3], [15, 10]]`, `Aₛₛ = [[15, -10], [100, 0]]`

**1. `A*ₖₖ = A⁻¹ₖₖ`:**
   `det(Aₖₖ) = 2*2 - (-1)*3 = 7`
   `A*ₖₖ = (1/7) * [[2, 1], [-3, 2]] ≈ [[0.2857, 0.1429], [-0.4286, 0.2857]]`

**2. `A*ₖₛ = A*ₖₖ * Aₖₛ`:**
   `A*ₖₛ = (1/7) * [[2, 1], [-3, 2]] * [[3, 1], [15, 10]] = (1/7) * [[21, 12], [21, 17]] = [[3, 1.7143], [3, 2.4286]]`

**3. `A'ₛₛ = Aₛₛ - Aₛₖ * A*ₖₛ`:**
   `Aₛₖ * A*ₖₛ = [[2, 3], [15, 10]] * [[3, 1.7143], [3, 2.4286]] = [[15, 10.7143], [75, 50]]`
   `A'ₛₛ = [[15, -10], [100, 0]] - [[15, 10.7143], [75, 50]] = [[0, -20.7143], [25, -50]]`

**Результат:** Модифікований блок (доповнення Шура) `A'ₛₛ ≈ [[0, -20.71], [25, -50]]`.

## 2. Приклад розрахунку на C#
Код демонструє повний алгоритм блочної факторизації для розв'язання СЛАР `M*x = y`.

```csharp
// Program.cs
using System;
using MathNet.Numerics.LinearAlgebra;
using MathNet.Numerics.LinearAlgebra.Double;

public class Program
{
    public static void Main(string[] args)
    {
        // --- Вхідні дані ---
        var M = DenseMatrix.OfArray(new double[,] {
            { 2, -1, 3, 1 }, { 3, 2, 15, 10 },
            { 2, 3, 15, -10 }, { 15, 10, 100, 0 }
        });
        var y = DenseVector.OfArray(new double[] { 1, 1, 1, 1 });

        Console.WriteLine("Початкова матриця М:\n" + M.ToMatrixString("F4"));

        // --- Алгоритм блочної факторизації ---

        // 1. Розбиваємо матрицю M та вектор y на блоки
        var Akk = M.SubMatrix(0, 2, 0, 2);
        var Aks = M.SubMatrix(0, 2, 2, 2);
        var Ask = M.SubMatrix(2, 2, 0, 2);
        var Ass = M.SubMatrix(2, 2, 2, 2);
        var Yk = y.SubVector(0, 2);
        var Ys = y.SubVector(2, 2);
        
        // 2. Прямий хід: обчислюємо проміжні матриці
        // A*kk = Akk^-1
        var Akk_inv = Akk.Inverse();
        // A*ks = A*kk * Aks
        var Aks_star = Akk_inv.Multiply(Aks);
        // A*sk = Ask * A*kk
        var Ask_star = Ask.Multiply(Akk_inv);
        // A'ss = Ass - Ask * A*ks (доповнення Шура)
        var Ass_prime = Ass - Ask_star.Multiply(Aks);
        // Y's = Ys - A*sk * Yk
        var Ys_prime = Ys - Ask_star.Multiply(Yk);

        // 3. Зворотний хід: знаходимо розв'язок
        // Розв'язуємо меншу систему: Xs = (A'ss)^-1 * Y's
        var Xs = Ass_prime.Solve(Ys_prime);
        // Знаходимо Xk, використовуючи вже відомий Xs
        var Xk = Akk_inv.Multiply(Yk - Aks.Multiply(Xs));

        // 4. Об'єднуємо результат в один вектор
        var x_block = Vector<double>.Build.Dense(y.Count);
        x_block.SetSubVector(0, 2, Xk);
        x_block.SetSubVector(2, 2, Xs);
        
        Console.WriteLine("\n--- Результат (блочний метод) ---");
        Console.WriteLine("x = " + x_block.ToVectorString("F4"));

        // --- Перевірка (розв'язок прямим методом для порівняння) ---
        var x_direct = M.Solve(y);
        Console.WriteLine("\n--- Результат (прямий метод) ---");
        Console.WriteLine("x = " + x_direct.ToVectorString("F4"));
        Console.WriteLine($"\nРізниця між методами (норма L2): {x_block.Subtract(x_direct).L2Norm():F10}");
    }
}
```

### 3. Перевірка правильності обрахунку
1.  **Проміжні результати:** Порівняйте значення `Akk_inv`, `Aks_star`, `Ass_prime`, отримані програмою, з вашим ручним розрахунком.
2.  **Фінальний результат:** Вектор `x_block` має майже ідеально збігатися з вектором `x_direct`.

## 4. Завдання для самостійної роботи
1. Адаптуйте код для розв'язання СЛАР 6x6, розбивши матрицю на блоки: `Akk` (4x4), `Aks` (4x2), `Ask` (2x4), `Ass` (2x2).
2. Для вашого варіанту, створіть матрицю `M` та вектор `y`.
3. Розв'яжіть систему блочним методом та перевірте результат за допомогою прямого методу.

> **Підказка для створення матриць:**
> Щоб створити діагональну матрицю, як у завданні, ви можете використати такий підхід:
> ```csharp
> int size = 6;
> var M = DenseMatrix.Create(size, size, 0.0); // Створити матрицю з нулів
> for (int i = 0; i < size; i++)
> {
>     M[i, i] = (i + 1) * 2 + 1; // Приклад для варіанту 1: diag(1..6)*2 + 1
> }
> ```

**Матриця `M` та вектор `y` для варіантів:**

| Вар | `M` (діагональні елементи) | `y` |
|---|---|---|
| 1 | `diag(1..6)*2 + 1` | `[1,2,3,4,5,6]` |
| 2 | `diag(6..1)*2 + 1` | `[6,5,4,3,2,1]` |
| 3 | `diag(1,3,5,2,4,6)*2 + 1.5` | `[10,2,8,4,6,5]` |
| 4 | `diag(6,4,2,5,3,1)*2 + 0.5` | `[1,1,2,2,3,3]` |
| 5 | `diag(2,2,3,3,4,4)*3 - 1` | `[9,8,7,6,5,4]` |
| 6 | `diag(10..15) + 2` | `[1,0,1,0,1,0]` |
| 7 | `diag(15..10) + 2.5` | `[0,1,0,1,0,1]` |
| 8 | `diag(1,1,1,5,5,5)*2 + 1` | `[2,4,6,8,10,12]`|
| 9 | `diag(10,1,9,2,8,3)*1.5 + 1`| `[12,10,8,6,4,2]` |
| 10 | `diag(1,10,2,9,3,8)*1.5 - 0.5`|`[3,6,9,12,9,6]` |
