# Лекція 20: Оцінка стану на основі синхрофазорних вимірювань (PMU / WAMS)

## 1. Теоретична частина

### Від нелінійної до лінійної оцінки стану (Linear State Estimation)
Традиційна оцінка стану (State Estimation) базується на вимірюваннях активної та реактивної потужності ($P$, $Q$) та модулів напруги ($|V|$). Оскільки потужність є нелінійною функцією від напруги ($S = V I^* = V (Y V)^*$), рівняння традиційної оцінки стану є нелінійними, і для їх розв'язання використовують ітераційні методи (наприклад, метод Гауса-Ньютона).

Поява **PMU (Phasor Measurement Units)** — пристроїв синхрофазорних вимірювань, що є основою систем WAMS (Wide Area Measurement Systems), змінила цю парадигму. PMU здатні з високою частотою (до 50-60 разів на секунду) вимірювати не просто модулі, а **комплексні вектори (фазори)** напруг та струмів, синхронізовані за допомогою GPS. 

Якщо наша система збору даних отримує виключно синхрофазори напруг та струмів, рівняння вимірювань стають повністю лінійними відносно комплексних напруг вузлів:
$$z = H \cdot x + e$$
де:
- $z$ — вектор комплексних вимірювань (напруги та струми від PMU).
- $x$ — вектор стану (комплексні напруги вузлів).
- $H$ — матриця вимірювань (містить 1 і 0 для напруг, та провідності ліній $y$ для струмів).
- $e$ — вектор похибок вимірювань.

Розв'язок задачі зважених найменших квадратів (WLS — Weighted Least Squares) для лінійної системи знаходиться за один крок (без ітерацій):
$$x = (H^H W H)^{-1} H^H W z$$
де $H^H$ — ермітове спряження (транспонування та комплексне спряження) матриці $H$, а $W$ — діагональна матриця ваг (обернено пропорційних дисперсіям похибок).

> **Нотатка від професора:**
> Лінійна оцінка стану — це мрія диспетчера. Вона не має проблем із збіжністю, не потребує початкового наближення (Flat Start) і виконується блискавично. Проте, на практиці мережі рідко мають 100% покриття PMU. Тому сучасні EMS (Energy Management Systems) використовують "гібридну" оцінку стану, де лінійні вимірювання від PMU поєднуються з традиційними нелінійними вимірюваннями SCADA, значно покращуючи стійкість ітераційного процесу.

> **Глибока інтуїція: Системна надмірність (Redundancy) та фільтрація шуму**
> Навіщо нам взагалі оцінка стану, якщо PMU і так вимірюють напруги вузлів? Справа в тому, що будь-який датчик має похибку (шум). Завдяки тому, що ми додатково вимірюємо струми ліній (надмірні вимірювання), матриця вимірювань $H$ має більше рядків, ніж стовпців. Операція $(H^H W H)^{-1} H^H$ діє як математичний "фільтр" — вона натягує суперечливі зашумлені дані на жорсткий фізичний каркас законів Кірхгофа (закладених у матриці $H$), видаючи найбільш ймовірний дійсний стан системи. Математика буквально "видавлює" шум з вимірювань!

### Приклад ручного розрахунку
Розглянемо мережу з 2 вузлів. Провідність лінії між вузлами 1 і 2 становить $y_{12} = -j10$.
Датчики PMU виміряли:
1. Напругу у вузлі 1: $V_1^{meas} = 1.0 + j0$
2. Напругу у вузлі 2: $V_2^{meas} = 1.0 - j0.1$
3. Струм від вузла 1 до 2: $I_{12}^{meas} = 1.0 + j0$ (фізично $I_{12} = y_{12}(V_1 - V_2)$).

Вектор вимірювань:
$$z = \begin{bmatrix} 1.0 \\ 1.0 - j0.1 \\ 1.0 \end{bmatrix}$$

Матриця вимірювань $H$ (перші два рядки — вимірювання напруг, третій — струм $I_{12} = y_{12}V_1 - y_{12}V_2$):
$$H = \begin{bmatrix} 1 & 0 \\ 0 & 1 \\ -j10 & j10 \end{bmatrix}$$

Приймемо матрицю ваг як одиничну ($W = I$). Знайдемо матрицю $G = H^H H$:
$$H^H = \begin{bmatrix} 1 & 0 & j10 \\ 0 & 1 & -j10 \end{bmatrix}$$
$$G = H^H H = \begin{bmatrix} 1 & 0 & j10 \\ 0 & 1 & -j10 \end{bmatrix} \begin{bmatrix} 1 & 0 \\ 0 & 1 \\ -j10 & j10 \end{bmatrix} = \begin{bmatrix} 1 - (j10)(-j10) & (j10)(j10) \\ (-j10)(-j10) & 1 - (-j10)(j10) \end{bmatrix} = \begin{bmatrix} 101 & -100 \\ -100 & 101 \end{bmatrix}$$

Знайдемо праву частину $rhs = H^H z$:
$$H^H z = \begin{bmatrix} 1 & 0 & j10 \\ 0 & 1 & -j10 \end{bmatrix} \begin{bmatrix} 1.0 \\ 1.0 - j0.1 \\ 1.0 \end{bmatrix} = \begin{bmatrix} 1.0 + j10 \\ 1.0 - j0.1 - j10 \end{bmatrix} = \begin{bmatrix} 1.0 + j10 \\ 1.0 - j10.1 \end{bmatrix}$$

Розв'яжемо систему $G \cdot x = rhs$. Визначник матриці $G$: $\det(G) = 101 \times 101 - 100 \times 100 = 201$.
Обернена матриця $G^{-1} = \frac{1}{201} \begin{bmatrix} 101 & 100 \\ 100 & 101 \end{bmatrix}$.

Оцінений стан $x = \begin{bmatrix} V_1 \\ V_2 \end{bmatrix}$:
$$V_1 = \frac{101(1 + j10) + 100(1 - j10.1)}{201} = \frac{101 + j1010 + 100 - j1010}{201} = \frac{201}{201} = 1.0 + j0$$
$$V_2 = \frac{100(1 + j10) + 101(1 - j10.1)}{201} = \frac{100 + j1000 + 101 - j1020.1}{201} = \frac{201 - j20.1}{201} = 1.0 - j0.1$$

Математика бездоганно відфільтрувала стан!

## 2. Приклад розрахунку на C#
Нижче наведено код на `MathNet.Numerics`, що реалізує лінійну оцінку стану для комплексних значень.

```csharp
// Program.cs
using System;
using Complex = System.Numerics.Complex;
using MathNet.Numerics.LinearAlgebra;

public class Program
{
    public static void Main(string[] args)
    {
        Console.WriteLine("--- Оцінка стану PMU/WAMS ---");

        // Вектор вимірювань: z = [V1, V2, I12]^T
        var z = Vector<Complex>.Build.DenseOfArray(new Complex[] {
            new Complex(1.0, 0.0),
            new Complex(1.0, -0.1),
            new Complex(1.0, 0.0)
        });

        // Провідність лінії y12 = -j10
        Complex y12 = new Complex(0.0, -10.0);

        // Матриця вимірювань H
        var H = Matrix<Complex>.Build.DenseOfArray(new Complex[,] {
            { new Complex(1, 0), new Complex(0, 0) },
            { new Complex(0, 0), new Complex(1, 0) },
            { y12, -y12 }
        });

        // Вагова матриця W (одинична)
        var W = Matrix<Complex>.Build.DenseIdentity(3);

        // Ермітове спряження H^H
        var H_H = H.ConjugateTranspose();

        // Матриця Грама G = H^H * W * H
        var G = H_H * W * H;

        // Вектор правої частини rhs = H^H * W * z
        var rhs = H_H * W * z;

        // Розв'язок системи G * x = rhs
        var x = G.Solve(rhs);

        Console.WriteLine("Оцінені напруги (State Vector):");
        Console.WriteLine($"V1 = {x[0].Real:F4} + j{x[0].Imaginary:F4}");
        Console.WriteLine($"V2 = {x[1].Real:F4} + j{x[1].Imaginary:F4}");
    }
}
```

## 3. Перевірка правильності обрахунку
1. Скомпілюйте і запустіть програму. Оцінені напруги `V1` та `V2` повинні збігатися з ручним розрахунком: `V1 = 1.0000 + j0.0000`, `V2 = 1.0000 + j-0.1000`.
2. Спробуйте змінити значення виміряного струму `I12` (наприклад, додати шум). Зверніть увагу, як зміна одного вимірювання "розмазується" по результату завдяки методу найменших квадратів, не призводячи до критичної помилки в напругах. Це і є ефект фільтрації.

## 4. Завдання для самостійної роботи
Обчисліть оцінений стан для свого варіанту. Використайте програму на C#, щоб знайти оцінені комплексні напруги `V1` та `V2`. Порівняйте вплив шуму в різних варіантах.

В усіх варіантах вимірюємо напруги у вузлах 1 і 2 та струм лінії 1-2. Провідність лінії $y_{12} = -j10$.
Вектор вимірювань задано як $z = [V_1, V_2, I_{12}]^T$. Вагова матриця $W$ — одинична.

| Вар | $V_1$ (p.u.) | $V_2$ (p.u.) | $I_{12}$ (p.u.) |
|---|---|---|---|
| 1 | `1.02 + j0.00` | `0.98 - j0.05` | `0.5 + j0.4` |
| 2 | `1.01 + j0.01` | `0.97 - j0.04` | `0.4 + j0.5` |
| 3 | `1.03 + j0.00` | `0.99 - j0.06` | `0.6 + j0.3` |
| 4 | `1.00 + j0.02` | `0.96 - j0.03` | `0.3 + j0.6` |
| 5 | `1.04 - j0.01` | `1.00 - j0.07` | `0.7 + j0.2` |
| 6 | `0.99 + j0.00` | `0.95 - j0.04` | `0.4 + j0.4` |
| 7 | `1.05 + j0.01` | `1.01 - j0.08` | `0.8 + j0.3` |
| 8 | `0.98 - j0.01` | `0.94 - j0.05` | `0.3 + j0.5` |
| 9 | `1.01 + j0.03` | `0.97 - j0.02` | `0.2 + j0.7` |
| 10| `1.02 - j0.02` | `0.98 - j0.09` | `0.9 + j0.1` |
