# Лекція 18: Несиметричні трифазні розріджені моделі розподільчих мереж (координати a-b-c)

## 1. Теоретична частина

### Від симетрії до суворої реальності
Досі у всіх наших розрахунках ми неявно робили фундаментальне припущення: три фази енергосистеми завантажені однаково. Тому ми моделювали всю мережу в однолінійному виконанні (Single-Line Diagram), використовуючи однофазні еквіваленти для кожної підстанції.
Це припущення чудово працює для магістральних мереж (330–750 кВ), де навантаження цілих міст статистично усереднюється і фази майже симетричні.

Проте, коли ми спускаємося на рівень **розподільчих мереж** (0.4–10 кВ), картина кардинально змінюється:
1. **Однофазні навантаження:** На одній вулиці до фази `a` підключено 10 будинків, до `b` — 15, до `c` — 5. Система глибоко несиметрична.
2. **Невзаємні лінії:** Проводи висять на стовпах на різній відстані один від одного. Взаємна індуктивність між фазами `a-b` не дорівнює `b-c`.
3. **Неповнофазні режими:** Обрив одного проводу (наприклад, фази `a`) залишає фази `b` і `c` у роботі.

Для точного аналізу таких режимів однолінійна модель марна. Нам потрібна **трифазна a-b-c модель**.

### Блочно-розріджені матриці
У трифазній моделі кожен фізичний вузол $i$ описується не одним комплексним числом напруги $V_i$, а вектором-стовпцем із трьох напруг:
$$V_i = \begin{bmatrix} V_i^a \\ V_i^b \\ V_i^c \end{bmatrix}$$

Відповідно, кожна лінія електропередавання між вузлами $i$ та $j$ описується не одним скалярним опором, а матрицею провідностей розміром $3 \times 3$:
$$Y_{ij}^{abc} = \begin{bmatrix} y_{aa} & y_{ab} & y_{ac} \\ y_{ba} & y_{bb} & y_{bc} \\ y_{ca} & y_{cb} & y_{cc} \end{bmatrix}$$
де $y_{aa}$ — власна провідність фази `a`, а $y_{ab}$ — взаємна індуктивна провідність між проводами `a` та `b`.

Глобальна матриця вузлових провідностей $Y_{bus}$ стає **блочно-розрідженою (Block-Sparse)**. Якщо в мережі $N$ вузлів, розмір матриці складатиме $3N \times 3N$. Кожен ненульовий "елемент" матриці тепер — це щільний блок $3 \times 3$.

> **Нотатка від професора:**
> Якщо лінія є симетричною, матриця $3 \times 3$ має однакові елементи на діагоналі і однакові позадіагональні елементи. Якщо стається обрив фази `a`, ми просто обнуляємо перший рядок і перший стовпець відповідного матричного блоку $Y_{ij}^{abc}$. Трифазна модель неймовірно гнучка!

> **Глибока інтуїція: Симетричні складові чи Фазні координати?**
> Ви могли чути про метод симетричних складових (Пряма, Зворотна, Нульова послідовності), який традиційно викладають в університетах для розрахунку несиметрії. Цей метод був створений сто років тому математиком Фортеск'ю виключно для того, щоб діагоналізувати матриці і дозволити інженерам рахувати все **на логарифмічній лінійці**. 
> Сьогодні, з появою комп'ютерів і форматів CSR/CSC, метод симетричних складових для складних розподільчих мереж втратив сенс. Матриця в координатах `a-b-c` формується простіше, дозволяє легко моделювати нелінійні несиметричні навантаження, глухозаземлені нейтралі і обриви. Більшість сучасного ПЗ (наприклад, OpenDSS від EPRI) працює виключно у фазних координатах `a-b-c`, використовуючи швидкі розріджені розв'язувачі для блочних матриць.

### Приклад ручного розрахунку
Розглянемо найпростішу мережу: Вузол 1 (Джерело) з'єднаний з Вузлом 2 (Навантаження) лінією. 
Вектор напруги джерела (симетричний, в.о.):
$$V_1 = \begin{bmatrix} 1 \angle 0^\circ \\ 1 \angle -120^\circ \\ 1 \angle +120^\circ \end{bmatrix} = \begin{bmatrix} 1 \\ -0.5 - j0.866 \\ -0.5 + j0.866 \end{bmatrix}$$

Для спрощення нехай матриця провідності лінії є діагональною (нехтуємо взаємною індукцією між фазами), але опір фази `a` вдвічі більший через поганий контакт:
$Y_{12} = \begin{bmatrix} 5 & 0 & 0 \\ 0 & 10 & 0 \\ 0 & 0 & 10 \end{bmatrix}$

Струм споживання у вузлі 2: фаза `a` завантажена струмом 1.0, інші порожні.
$I_2 = \begin{bmatrix} -1 \\ 0 \\ 0 \end{bmatrix}$ (мінус, бо струм витікає з вузла).

Рівняння для вузла 2:
$$I_2 = Y_{21} V_1 + Y_{22} V_2$$
Оскільки $Y_{22} = -Y_{21} = Y_{12}$ (якщо немає шунтів), маємо:
$$Y_{12} V_2 = Y_{12} V_1 + I_2$$
$$\begin{bmatrix} 5 & 0 & 0 \\ 0 & 10 & 0 \\ 0 & 0 & 10 \end{bmatrix} \begin{bmatrix} V_2^a \\ V_2^b \\ V_2^c \end{bmatrix} = \begin{bmatrix} 5 & 0 & 0 \\ 0 & 10 & 0 \\ 0 & 0 & 10 \end{bmatrix} \begin{bmatrix} 1 \\ -0.5 - j0.866 \\ -0.5 + j0.866 \end{bmatrix} + \begin{bmatrix} -1 \\ 0 \\ 0 \end{bmatrix}$$

Для фази `a`:
$5 \cdot V_2^a = 5 \cdot 1 - 1 = 4 \implies V_2^a = 0.8 \text{ в.о.}$
Для фаз `b` і `c`: струму навантаження немає, падіння напруги нульове, тому $V_2^b = V_1^b$, $V_2^c = V_1^c$.
Напруга фази `a` просіла до 0.8 в.о., система стала несиметричною!

## 2. Приклад розрахунку на C#
Нижче наведено код, який формує трифазну розріджену матрицю $3N \times 3N$. Оскільки `MathNet.Numerics` працює з дійсними числами в базовій конфігурації, ми змоделюємо DC-еквівалент (тільки модулі і резистивні опори) для демонстрації блочної структури. (В реальності використовується `Complex` версія `Matrix<Complex>`).

```csharp
// Program.cs
using System;
using MathNet.Numerics.LinearAlgebra;

public class Program
{
    public static void Main(string[] args)
    {
        Console.WriteLine("--- Трифазна несиметрична модель (Координати a-b-c) ---");
        
        int numBuses = 2; 
        int N = numBuses * 3; // Розмірність матриці 3N x 3N
        
        // Створюємо розріджену матрицю 6x6
        var Ybus = Matrix<double>.Build.Sparse(N, N);
        
        // Матриця провідності ЛЕП (3x3)
        // Власні провідності фаз: a=5, b=10, c=10. Взаємоіндукція a-b = 1.0 (для прикладу)
        var Y_line = Matrix<double>.Build.DenseOfArray(new double[,] {
            { 5.0, -1.0,  0.0 },
            {-1.0, 10.0,  0.0 },
            { 0.0,  0.0, 10.0 }
        });
        
        // Індекси: Вузол 1 (0, 1, 2), Вузол 2 (3, 4, 5)
        int idx1 = 0 * 3;
        int idx2 = 1 * 3;
        
        // Додаємо блоки до глобальної матриці Ybus
        Ybus.SetSubMatrix(idx1, idx1, Y_line);         // Y_11
        Ybus.SetSubMatrix(idx2, idx2, Y_line);         // Y_22
        Ybus.SetSubMatrix(idx1, idx2, -1.0 * Y_line);  // Y_12
        Ybus.SetSubMatrix(idx2, idx1, -1.0 * Y_line);  // Y_21
        
        Console.WriteLine("Глобальна блочно-розріджена матриця Ybus (6x6):");
        Console.WriteLine(Ybus.ToMatrixString("F1"));
        
        // Задаємо напругу джерела (Вузол 1). Уявімо DC-амплітуди: 1.0 для всіх фаз
        var V1 = Vector<double>.Build.Dense(new double[] { 1.0, 1.0, 1.0 });
        
        // Задаємо струм навантаження Вузла 2 (Несиметричне! Тільки на фазі a)
        var I2 = Vector<double>.Build.Dense(new double[] { -1.0, 0.0, 0.0 });
        
        // Система для вузла 2: Y_22 * V2 = Y_line * V1 + I2
        var RHS = (Y_line * V1) + I2;
        
        // Знайдемо напруги вузла 2, розв'язавши блок 3x3
        var V2 = Y_line.Solve(RHS);
        
        Console.WriteLine("\nСтрум навантаження I2 (a, b, c):");
        Console.WriteLine(I2.ToVectorString("F2"));
        
        Console.WriteLine("\nНапруги у вузлі 2 V2 (a, b, c):");
        Console.WriteLine(V2.ToVectorString("F4"));
        
        // Зверніть увагу, що напруга на фазі 'b' також зміниться через взаємоіндукцію (y_ab = -1.0),
        // навіть незважаючи на те, що до неї не підключено навантаження!
    }
}
```

## 3. Перевірка правильності обрахунку
1. Скомпілюйте код. Подивіться на вивід матриці `Ybus`. Вона має чітку блокову структуру $2 \times 2$, де кожен блок має розмір $3 \times 3$.
2. Проаналізуйте напругу $V_2$. Через сильне навантаження на фазі `a`, $V_2^a$ має впасти найсильніше. Через взаємоіндукцію $y_{ab} = -1.0$, напруга фази `b` також відхилиться від ідеальної $1.0$, хоча її струм навантаження дорівнює нулю. Це і є головний ефект трифазних моделей, який губиться в однолінійних схемах.

## 4. Завдання для самостійної роботи
Змініть у програмі матрицю `Y_line` відповідно до вашого варіанту та струм навантаження $I_2$. Запишіть розрахований вектор напруг $V_2$.

В усіх варіантах напруга джерела $V_1 = [1.0, 1.0, 1.0]^T$.

| Вар | `Y_line` (власні $a, b, c$ та взаємна $ab=ba$) | Струм навантаження $I_2$ (a, b, c) |
|---|---|---|
| 1 | `a=4.0, b=8.0, c=8.0, ab=-0.5` | `[-2.0, 0.0, 0.0]` |
| 2 | `a=5.0, b=5.0, c=5.0, ab=-1.0` | `[-1.0, -1.0, 0.0]` |
| 3 | `a=3.0, b=9.0, c=9.0, ab=-0.8` | `[-1.5, 0.0, 0.0]` |
| 4 | `a=6.0, b=6.0, c=6.0, ab=-1.5` | `[-2.0, -0.5, 0.0]` |
| 5 | `a=4.5, b=7.0, c=7.0, ab=-0.6` | `[-1.0, 0.0, -1.0]` |
| 6 | `a=5.5, b=5.5, c=5.5, ab=-1.2` | `[-1.2, -0.8, 0.0]` |
| 7 | `a=3.5, b=8.5, c=8.5, ab=-0.7` | `[-1.8, 0.0, 0.0]` |
| 8 | `a=6.5, b=6.5, c=6.5, ab=-1.4` | `[-0.5, -2.0, 0.0]` |
| 9 | `a=4.2, b=7.5, c=7.5, ab=-0.9` | `[-1.5, -1.5, 0.0]` |
| 10| `a=5.2, b=5.2, c=5.2, ab=-1.1` | `[-1.0, 0.0, -1.5]` |
