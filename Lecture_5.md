# Лекція 5: Вступ до оптимізації режимів електричних мереж

## 1. Теоретична частина

### Постановка задачі оптимізації режиму

У попередніх лекціях ми розв'язували задачу розрахунку усталеного режиму: маючи дані про мережу та навантаження, ми знаходили напруги та потоки. Однак, в реальності існує безліч допустимих режимів, і завжди стоїть задача обрати **найкращий (оптимальний)** з них.

> **Нотатка від професора: Розрахунок vs. Оптимізація (Аналогія з GPS)**
>
> *   **Розрахунок режиму** — це як відповідь на питання: "Якщо я поїду з точки А в точку Б цим конкретним маршрутом, скільки часу це займе і скільки палива я витрачу?". Ми аналізуємо один заданий сценарій.
> *   **Оптимізація режиму** — це робота самого GPS-навігатора. Він аналізує *всі можливі* маршрути і знаходить той, який є найкращим за певним критерієм (найшвидший, найкоротший, з найменшою витратою палива), при цьому враховуючи обмеження (наприклад, не їхати по зустрічній смузі).

**Оптимізація режиму** — це процес знаходження такого стану енергосистеми, який є допустимим з технічної точки зору і при цьому мінімізує (або максимізує) обрану **цільову функцію**.

### Компоненти задачі оптимізації

Будь-яка задача оптимізації формально складається з трьох основних компонентів:

1.  **Цільова функція (Objective Function)**
    Це математичний вираз, який ми хочемо зробити якомога кращим. Найчастіше в енергетиці ми прагнемо **мінімізувати сумарні втрати активної потужності `ΔP_Σ`** в мережі. Це напряму пов'язано з економією палива на електростанціях та підвищенням ККД системи.

2.  **Керовані змінні (Control Variables)**
    Це "ручки та важелі", які диспетчер може крутити, щоб змінити режим роботи системи. До них належать:
    -   Активна та реактивна потужність генераторів.
    -   Напруга на шинах генераторних вузлів.
    -   Коефіцієнти трансформації регульованих трансформаторів.
    -   Потужність пристроїв компенсації (батарей конденсаторів, реакторів).

3.  **Обмеження (Constraints)**
    Це набір правил та законів, які не можна порушувати. Вони бувають двох типів:
    -   **Обмеження-рівності:** Це фундаментальні закони фізики, в нашому випадку - **рівняння усталеного режиму** (закони Кірхгофа, баланс потужностей). Вони мають виконуватись завжди і точно.
    -   **Обмеження-нерівності:** Це експлуатаційні та технічні межі обладнання. Вони задають допустимий діапазон роботи. Наприклад:
        -   Напруга у вузлах має бути в допустимих межах: `U_min ≤ U_вузла ≤ U_max`.
        -   Генератор не може видавати нескінченну реактивну потужність: `Q_min ≤ Q_gen ≤ Q_max`.
        -   Лінія електропередачі не може бути перевантажена: `P_лінії ≤ P_лінії_max`.

### Розрахунок втрат потужності в мережі
Сумарні втрати `ΔS_Σ` - це різниця між сумарною генерацією та сумарним навантаженням. Їх можна знайти, просумувавши всі інжекції потужності у вузли мережі.
`S_інжекції = U .* conj(Y * U)` (де `.*` - поелементне множення)
`ΔS_Σ = ΔP_Σ + jΔQ_Σ = sum(S_інжекції)`

### Приклад ручного розрахунку: Втрати в одній гілці
**Дано:** `U_i = 1.0∠0°`, `U_j = 0.98∠-5°`, `y_ij = 0.1 - j0.4`.

1.  **Напруги у комплексній формі:**
    `U_i = 1.0 + 0j`
    `U_j = 0.98 * (cos(-5°) + j*sin(-5°)) ≈ 0.9762 - j0.0854`
2.  **Струм від `i` до `j`:**
    `I_ij = (U_i - U_j) * y_ij = ((1.0 - 0.9762) + j(0 - (-0.0854))) * (0.1 - j0.4)`
    `I_ij = (0.0238 + j0.0854) * (0.1 - j0.4) ≈ 0.0365 - j0.0010`
3.  **Потік потужності від `i` до `j`:**
    `S_ij = U_i * conj(I_ij) = (1.0 + 0j) * (0.0365 + j0.0010) = 0.0365 + j0.0010`
4.  **Потік потужності від `j` до `i`:**
    `I_ji = -I_ij = -0.0365 + j0.0010`
    `S_ji = U_j * conj(I_ji) = (0.9762 - j0.0854) * (-0.0365 - j0.0010) ≈ -0.0356 + j0.0021`
5.  **Втрати = Сума потоків на кінцях лінії:**
    `ΔP_ij = P_ij + P_ji = 0.0365 - 0.0356 = 0.0009`
    `ΔQ_ij = Q_ij + Q_ji = 0.0010 + 0.0021 = 0.0031`

## 2. Приклад розрахунку на C#
Код демонструє, як розрахувати значення цільової функції (сумарних втрат) для **вже розрахованого** режиму.

```csharp
// Program.cs
using System;
using System.Numerics;
using MathNet.Numerics.LinearAlgebra;
using MathNet.Numerics.LinearAlgebra.Complex;

public class Program
{
    public static void Main(string[] args)
    {
        // --- Вхідні дані ---
        var y12 = new Complex(1, -4);
        var y13 = new Complex(2, -5);
        var y23 = new Complex(3, -6);
        var Y = Matrix<Complex>.Build.DenseOfArray(new Complex[,] {
            { y12 + y13, -y12, -y13 },
            { -y12, y12 + y23, -y23 },
            { -y13, -y23, y13 + y23 }
        });

        // Вектор напруг (припустимо, що режим вже розраховано)
        var U = Vector<Complex>.Build.DenseOfArray(new[] {
            Complex.FromPolarCoordinates(1.02, 0),
            Complex.FromPolarCoordinates(0.98, -3.5 * Math.PI / 180),
            Complex.FromPolarCoordinates(0.97, -4.1 * Math.PI / 180)
        });

        // --- Розрахунок сумарних втрат ---
        var (totalPloss, totalQloss) = CalculateTotalLosses(Y, U);
        Console.WriteLine("--- Сумарні втрати в мережі ---");
        Console.WriteLine($"Активні втрати ΔP_Σ: {totalPloss:F5} о.о.");
        Console.WriteLine($"Реактивні втрати ΔQ_Σ: {totalQloss:F5} о.о.");
    }

    /// <summary>
    /// Розраховує сумарні втрати в мережі за методом інжекцій.
    /// </summary>
    public static (double Ploss, double Qloss) CalculateTotalLosses(Matrix<Complex> yBus, Vector<Complex> voltages)
    {
        // Розраховуємо вектор вузлових струмів: I = Y * U
        var I_injections = yBus.Multiply(voltages);
        
        // Розраховуємо вектор інжекцій потужності: S = U .* conj(I)
        var S_injections = voltages.PointwiseMultiply(I_injections.Conjugate());
        
        // Сумарні втрати - це сума всіх комплексних інжекцій
        Complex totalSLoss = S_injections.Sum();
        
        return (totalSLoss.Real, totalSLoss.Imaginary);
    }
}
```

### 3. Перевірка правильності обрахунку
Для перевірки коду, ви можете написати окрему функцію для розрахунку втрат в гілці (як у ручному прикладі) і переконатись, що сума втрат по всіх гілках приблизно дорівнює загальним втратам, розрахованим методом інжекцій.

## 4. Завдання для самостійної роботи
Для вашого варіанту, що містить Y-матрицю та вектор напруг для мережі з 4-х вузлів, розробіть програму, яка розраховує та виводить на екран:
1.  Сумарні втрати активної потужності `ΔP_Σ`.
2.  Сумарні втрати реактивної потужності `ΔQ_Σ`.

**Y-матриця для всіх варіантів:**
Складається з провідностей гілок `y12=1-j4`, `y13=2-j5`, `y14=1-j3`, `y23=1-j2`, `y34=2-j4`.

**Вектор напруг `U = [U₁, U₂, U₃, U₄]` для варіантів (модуль, кут в градусах):**

| Вар | U₁ | U₂ | U₃ | U₄ |
|---|---|---|---|---|
| 1 | 1.05∠0 | 1.01∠-2.5 | 0.99∠-3.1 | 0.98∠-4.0 |
| 2 | 1.05∠0 | 1.02∠-2.1 | 0.98∠-3.5 | 0.97∠-4.2 |
| 3 | 1.04∠0 | 1.00∠-3.0 | 1.00∠-3.3 | 0.99∠-3.9 |
| 4 | 1.04∠0 | 0.99∠-3.2 | 0.97∠-4.0 | 0.96∠-4.5 |
| 5 | 1.03∠0 | 1.01∠-1.9 | 1.01∠-2.5 | 1.00∠-3.0 |
| 6 | 1.03∠0 | 0.98∠-3.5 | 0.96∠-4.1 | 0.95∠-5.0 |
| 7 | 1.02∠0 | 1.00∠-2.8 | 0.99∠-3.2 | 0.99∠-3.8 |
| 8 | 1.02∠0 | 0.97∠-4.0 | 0.95∠-4.8 | 0.94∠-5.5 |
| 9 | 1.01∠0 | 0.99∠-3.1 | 0.98∠-3.6 | 0.97∠-4.3 |
| 10 | 1.01∠0 | 1.00∠-2.2 | 1.00∠-2.4 | 1.00∠-2.9 |