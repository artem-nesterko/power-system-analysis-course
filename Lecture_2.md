# Лекція 2: Метод Ньютона для розрахунку потокорозподілу

## 1. Теоретична частина

### Рівняння стану електричної мережі

В основі всіх розрахунків лежить закон Ома у матричній формі, який ми вже знаємо: `[I] = [Y] * [U]`. Це рівняння пов'язує струми `[I]`, напруги `[U]` та параметри мережі `[Y]`.

Однак в енергетиці нас частіше цікавить не струм, а **потужність**. Комплексна потужність `S` у вузлі `i` визначається як `Sᵢ = Pᵢ + jQᵢ = Uᵢ * conj(Iᵢ)`, де `conj(Iᵢ)` - комплексно-спряжений струм. Підставивши сюди вираз для струму, ми отримаємо основне **нелінійне** рівняння потокорозподілу:

`[S] = diag([U]) * conj([Y] * [U])`

Це рівняння не можна розв'язати аналітично для великих мереж, тому що напруги `U` входять до нього в складній формі. Тому ми використовуємо ітераційні чисельні методи, найпотужнішим з яких є **метод Ньютона**.

### Типи вузлів та невідомі
- **Slack (балансуючий) вузол:** "Диспетчер" енергосистеми. Тут ми фіксуємо напругу (`|U|` та кут `δ`), а цей вузол бере на себе компенсацію всіх втрат у мережі.
- **PQ-вузол (навантаження):** Звичайний споживач. Ми знаємо, скільки активної (`P`) та реактивної (`Q`) потужності він споживає. Наша мета - знайти, яка при цьому встановиться напруга (`|U|` та `δ`).
- **PV-вузол (генераторний):** Електростанція. Ми задаємо, скільки активної потужності (`P`) вона генерує, і вимагаємо підтримувати певний рівень напруги (`|U|`). Невідомими є кут `δ` та кількість реактивної потужності `Q`, яку потрібно для цього згенерувати.

У цій лекції для спрощення розглядаємо лише Slack та PQ вузли.

### Суть методу Ньютона
Метод Ньютона зводить розв'язання складної нелінійної системи до послідовного розв'язання простіших **лінійних** систем.

**Алгоритм:**
1.  **Ініціалізація:** Задаємо початкові значення для всіх невідомих напруг `|U|` та кутів `δ`. Зазвичай беруть "плаский старт": `|U|=1.0`, `δ=0`.
2.  **Розрахунок нев'язок (помилок):** Розраховуємо, які потужності `P_розр`, `Q_розр` виходять при поточних напругах. Знаходимо різницю між тим, що має бути (`P_зад`, `Q_зад`) і тим, що ми розрахували. Це і є вектор нев'язок `ΔW`.
3.  **Перевірка збіжності:** Якщо всі нев'язки менші за задану точність (напр. `0.001`), ми знайшли розв'язок. Розрахунок завершено.
4.  **Формування матриці Якобі [J]**: Це найважливіший крок.
5.  **Розв'язання лінійної системи** `[J] * [ΔX] = [ΔW]`. В результаті ми знаходимо вектор поправок `ΔX` (на скільки потрібно змінити наші `|U|` та `δ`).
6.  **Оновлення змінних:** Коригуємо напруги: `X_нова = X_стара + ΔX`.
7.  Повертаємось до кроку 2.

> **Нотатка: Що таке матриця Якобі?**
>
> Уявіть, що ви стоїте на схилі гори і хочете спуститися вниз. Куди зробити крок? Матриця Якобі – це ваш "навігатор". Вона відповідає на питання: "Якщо я трохи зміню напругу у вузлі А, як сильно це вплине на потужність у вузлі Б?".
>
> Технічно, це матриця **чутливостей** (часткових похідних). Вона показує, як зміна кожної з невідомих (`|U|`, `δ`) впливає на кожну з розрахованих величин (`P`, `Q`). Знаючи це, метод Ньютона може обчислити оптимальний "крок" для наближення до правильного розв'язку.

### Приклад ручного розрахунку: Вектор нев'язок
Розрахуємо вектор нев'язок `ΔW` для першої ітерації.
- **Вузол 1:** Балансуючий, `U₁ = 1.0∠0°`.
- **Вузол 2:** Навантаження. Задана потужність: `P₂_зад = -0.5`, `Q₂_зад = -0.2` (знак "мінус", бо це споживання).
- **Провідність лінії 1-2:** `y₁₂ = 1 - 4j`.
- **Y-матриця:** `Y₁₁=1-4j`, `Y₂₂=1-4j`, `Y₁₂=Y₂₁=-(1-4j)=-1+4j`.
- **Початкові умови:** "плаский старт" `U₁ = 1+0j`, `U₂ = 1+0j`.

**Розрахунок потужності у вузлі 2 на першій ітерації:**
1.  Струм, що втікає у вузол 2:
    `I₂ = Y₂₁*U₁ + Y₂₂*U₂ = (-1+4j)*(1+0j) + (1-4j)*(1+0j) = -1+4j + 1-4j = 0+0j`.
2.  Розрахункова потужність:
    `S₂_розр = U₂ * conj(I₂) = (1+0j) * conj(0+0j) = 0+0j`.
    Отже, `P₂_розр = 0`, `Q₂_розр = 0`.
3.  Нев'язки:
    `ΔP₂ = P₂_зад - P₂_розр = -0.5 - 0 = -0.5`
    `ΔQ₂ = Q₂_зад - Q₂_розр = -0.2 - 0 = -0.2`

**Результат:** Вектор нев'язок `ΔW = [-0.5, -0.2]`. Саме цю помилку має виправити перша ітерація методу.

## 2. Приклад розрахунку на C#

Реалізуємо повний ітераційний процес для 2-вузлової схеми.
- **Вузол 1:** Балансуючий, `U₁ = 1.0∠0°`.
- **Вузол 2:** Навантаження, `P₂ = 0.5`, `Q₂ = 0.2`.
- **Лінія 1-2:** Провідність `y₁₂ = 1 - 4j`.
- **Невідомі:** `|U₂|` та `δ₂`.

```csharp
// Program.cs
using System;
using System.Numerics;
using MathNet.Numerics.LinearAlgebra;
using MathNet.Numerics.LinearAlgebra.Complex;

public class Program
{
    public static void Main(string[] args)
    {
        // --- 1. Вхідні дані ---
        var y12 = new Complex(1, -4);
        var YMatrix = Matrix<Complex>.Build.DenseOfArray(new Complex[,] {
            { y12, -y12 },
            { -y12, y12 }
        });

        // Задані потужності у вузлі 2 (зі знаком мінус, бо це навантаження/відтік потужності)
        double P2_spec = -0.5;
        double Q2_spec = -0.2;

        // Задана напруга у балансуючому вузлі 1
        var U1 = Complex.FromPolarCoordinates(1.0, 0.0);

        // --- 2. Ініціалізація ---
        // Початкове наближення для невідомої напруги у вузлі 2 ("плаский старт")
        var U2 = Complex.FromPolarCoordinates(1.0, 0.0);
        
        int maxIterations = 10;
        double tolerance = 0.001; // Точність розрахунку

        Console.WriteLine("--- Початок ітераційного процесу методу Ньютона ---");

        for (int i = 0; i < maxIterations; i++)
        {
            Console.WriteLine($"\nІтерація {i + 1}:");
            Console.WriteLine($"U2 = {U2.Magnitude:F4} ∠ {U2.Phase * 180 / Math.PI:F4}°");

            // --- Крок 1: Розрахунок нев'язок (ΔP, ΔQ) ---
            var I2_calc = YMatrix[1, 0] * U1 + YMatrix[1, 1] * U2;
            var S2_calc = U2 * Complex.Conjugate(I2_calc);

            double dP = P2_spec - S2_calc.Real;
            double dQ = Q2_spec - S2_calc.Imaginary;
            Console.WriteLine($"Нев'язка: dP = {dP:F5}, dQ = {dQ:F5}");

            // --- Крок 2: Перевірка збіжності ---
            if (Math.Abs(dP) < tolerance && Math.Abs(dQ) < tolerance)
            {
                Console.WriteLine("\nРозрахунок зійшовся!");
                break;
            }

            // --- Крок 3: Формування матриці Якобі [J] ---
            // Це матриця чутливостей [ [dP/dδ, dP/d|U|], [dQ/dδ, dQ/d|U|] ]
            double V2 = U2.Magnitude;
            double d2 = U2.Phase;
            var G21 = YMatrix[1,0].Real;
            var B21 = YMatrix[1,0].Imaginary;
            var G22 = YMatrix[1,1].Real;
            var B22 = YMatrix[1,1].Imaginary;

            // dP2/ddelta2: Як P2 реагує на зміну кута d2
            double J11 = V2 * U1.Magnitude * (-G21 * Math.Sin(d2 - U1.Phase) + B21 * Math.Cos(d2 - U1.Phase));
            // dP2/dV2: Як P2 реагує на зміну модуля напруги V2
            double J12 = 2 * V2 * G22 + U1.Magnitude * (G21 * Math.Cos(d2 - U1.Phase) + B21 * Math.Sin(d2 - U1.Phase));
            // dQ2/ddelta2: Як Q2 реагує на зміну кута d2
            double J21 = V2 * U1.Magnitude * (G21 * Math.Cos(d2 - U1.Phase) + B21 * Math.Sin(d2 - U1.Phase));
            // dQ2/dV2: Як Q2 реагує на зміну модуля напруги V2
            double J22 = -2 * V2 * B22 + U1.Magnitude * (G21 * Math.Sin(d2 - U1.Phase) - B21 * Math.Cos(d2 - U1.Phase));

            var J = Matrix<double>.Build.DenseOfArray(new double[,] { { J11, J12 }, { J21, J22 } });
            var mismatchVector = MathNet.Numerics.LinearAlgebra.Vector<double>.Build.Dense(new[] { dP, dQ });

            // --- Крок 4: Розв'язання СЛАР та оновлення змінних ---
            // Знаходимо поправки [dDelta, dV] = J.Solve([dP, dQ])
            var corrections = J.Solve(mismatchVector);
            
            // Оновлюємо напругу: додаємо розраховані поправки
            U2 = Complex.FromPolarCoordinates(V2 + corrections[1], d2 + corrections[0]);

            if (i == maxIterations - 1) Console.WriteLine("\nПеревищено максимальну кількість ітерацій.");
        }

        Console.WriteLine("\n--- Результат ---");
        Console.WriteLine($"Напруга у вузлі 2: |U2| = {U2.Magnitude:F5} о.о., δ2 = {U2.Phase * 180 / Math.PI:F5}°");
    }
}
```

### 3. Перевірка правильності обрахунку
1.  **Перша ітерація:** Переконайтесь, що нев'язки `dP` та `dQ` на першій ітерації збігаються з ручним розрахунком.
2.  **Збіжність:** Програма має зійтись за 3-5 ітерацій. Якщо цього не відбувається, найімовірніше, є помилка у формулах для матриці Якобі.
3.  **Фінальний результат:** Підставте отримані `|U₂|` та `δ₂` назад у формулу розрахунку потужності `S₂_розр`. Результат має бути дуже близьким до заданих `-0.5` та `-0.2`.

## 4. Завдання для самостійної роботи

Модифікуйте наведений код для розрахунку режиму в мережі з 3-х вузлів.
- **Вузол 1:** Балансуючий, `U₁ = 1.02∠0°`.
- **Вузли 2, 3:** Навантаження (PQ).

**Що потрібно зробити:**
1.  Сформувати Y-матрицю 3x3.
2.  Розширити вектор нев'язок до 4-х елементів: `[ΔP₂, ΔQ₂, ΔP₃, ΔQ₃]`.
3.  Розширити матрицю Якобі до розмірності 4x4.

> **Підказка по структурі Якобіана 4x4:**
> Ваші невідомі (вектор `X`): `[δ₂, δ₃, |U₂|, |U₃|]`.
> Ваші рівняння (вектор `W`): `[P₂, Q₂, P₃, Q₃]`.
> Матриця `J` матиме вигляд:
> ```
> [[ ∂P₂/∂δ₂  ∂P₂/∂δ₃  |  ∂P₂/∂|U₂|  ∂P₂/∂|U₃| ]
>  [ ∂Q₂/∂δ₂  ∂Q₂/∂δ₃  |  ∂Q₂/∂|U₂|  ∂Q₂/∂|U₃| ]
>  [ ------------------+------------------ ]
>  [ ∂P₃/∂δ₂  ∂P₃/∂δ₃  |  ∂P₃/∂|U₂|  ∂P₃/∂|U₃| ]
>  [ ∂Q₃/∂δ₂  ∂Q₃/∂δ₃  |  ∂Q₃/∂|U₂|  ∂Q₃/∂|U₃| ]]
> ```
> Формули для розрахунку елементів потрібно буде узагальнити з прикладу або знайти в теоретичних матеріалах.

**Параметри для варіантів:**
- **Схема:** 3 вузли; гілки 1-2, 1-3, 2-3.
- **Провідності (однакові для всіх):** `y₁₂ = 1-4j`, `y₁₃ = 2-5j`, `y₂₃ = 3-6j`.
- Навантаження в таблиці задані як **додатні значення споживання**. У коді їх потрібно вказувати зі знаком "мінус".

| Вар | `P_L2` | `Q_L2` | `P_L3` | `Q_L3` |
|---:|---:|---:|---:|---:|
| 1 | 0.50 | 0.20 | 0.30 | 0.10 |
| 2 | 0.60 | 0.25 | 0.20 | 0.08 |
| 3 | 0.40 | 0.15 | 0.35 | 0.12 |
| 4 | 0.55 | 0.30 | 0.25 | 0.10 |
| 5 | 0.45 | 0.18 | 0.40 | 0.15 |
| 6 | 0.70 | 0.28 | 0.30 | 0.12 |
| 7 | 0.35 | 0.12 | 0.25 | 0.09 |
| 8 | 0.65 | 0.22 | 0.45 | 0.16 |
| 9 | 0.50 | 0.10 | 0.50 | 0.20 |
| 10 | 0.30 | 0.08 | 0.60 | 0.25 |
