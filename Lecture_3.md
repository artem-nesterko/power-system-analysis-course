# Лекція 3: Матриця Якобі та її формування

## 1. Теоретична частина

### Роль якобіана в методі Ньютона
Як ми з'ясували у попередній лекції, ключовим елементом методу Ньютона є **матриця Якобі `[J]`**. Вона є "мозковим центром" операції, що дозволяє лінеаризувати складну нелінійну задачу в поточній точці і визначити напрямок для наступного кроку.

`[J] * [ΔX] = [ΔW]`

Тобто, знаючи поточну помилку `[ΔW]` та "чутливість" системи `[J]`, ми можемо знайти поправки `[ΔX]`, які цю помилку виправлять.

### Структура та фізичний сенс матриці Якобі
Для системи, де вузол 1 - балансуючий, а решта `N-1` - це PQ-вузли, Якобіан має розмірність `2(N-1) x 2(N-1)`. Його зручно уявити у вигляді чотирьох блоків:
```
      | ∂P/∂δ   ∂P/∂|U| |
J  =  |-----------------|
      | ∂Q/∂δ   ∂Q/∂|U| |
```
- **∂P/∂δ, ∂Q/∂δ:** Показують, як активна/реактивна потужність реагує на зміну **кутів** напруг.
- **∂P/∂|U|, ∂Q/∂|U|:** Показують, як активна/реактивна потужність реагує на зміну **модулів** напруг.

**Фізичний сенс елементів:**
- **Діагональні елементи** (напр., `∂P₂/∂δ₂`, `∂P₂/∂|U₂|`): Описують вплив параметрів вузла (його власних кута та напруги) на потужність **у цьому ж вузлі**. Цей зв'язок зазвичай дуже сильний.
- **Недіагональні елементи** (напр., `∂P₂/∂δ₃`, `∂P₂/∂|U₃|`): Описують вплив параметрів **сусіднього вузла `j`** на потужність у вузлі `i`. Цей зв'язок існує тільки якщо між вузлами є фізична лінія, і він, як правило, слабший за діагональний.

### Формули для розрахунку елементів якобіана
`Yᵢⱼ = Gᵢⱼ + jBᵢⱼ`, `Uᵢ = |Uᵢ|∠δᵢ`.

#### Недіагональні елементи (i ≠ j) - зв'язок між різними вузлами
`dPᵢ/dδⱼ = |Uᵢ||Uⱼ|(Gᵢⱼ sin(δᵢ-δⱼ) - Bᵢⱼ cos(δᵢ-δⱼ))`
`dPᵢ/d|Uⱼ| = |Uᵢ|(Gᵢⱼ cos(δᵢ-δⱼ) + Bᵢⱼ sin(δᵢ-δⱼ))`
`dQᵢ/dδⱼ = -|Uᵢ||Uⱼ|(Gᵢⱼ cos(δᵢ-δⱼ) + Bᵢⱼ sin(δᵢ-δⱼ))`
`dQᵢ/d|Uⱼ| = |Uᵢ|(Gᵢⱼ sin(δᵢ-δⱼ) - Bᵢⱼ cos(δᵢ-δⱼ))`

#### Діагональні елементи (i = j) - зв'язок всередині вузла
`dPᵢ/dδᵢ = -Qᵢ_розр - Bᵢᵢ|Uᵢ|²`
`dPᵢ/d|Uᵢ| = (Pᵢ_розр / |Uᵢ|) + Gᵢᵢ|Uᵢ|`
`dQᵢ/dδᵢ = Pᵢ_розр - Gᵢᵢ|Uᵢ|²`
`dQᵢ/d|Uᵢ| = (Qᵢ_розр / |Uᵢ|) - Bᵢᵢ|Uᵢ|`

> **Нотатка від професора: Чому метод Ньютона "дорогий"?**
>
> Зверніть увагу на формули для діагональних елементів. Вони залежать від `Pᵢ_розр` та `Qᵢ_розр` - розрахункових потужностей на **поточній ітерації**. Це означає, що матриця Якобі **не є константою**. Її потрібно повністю перераховувати на кожному кроці ітераційного процесу. Саме ця операція (формування та розв'язання системи з матрицею Якобі) є найбільш обчислювально затратною в усьому алгоритмі.

### Приклад ручного розрахунку елементів
Розрахуємо два елементи якобіана для "плаского старту" у 3-вузловій системі (вузол 1 - баланс, 2 і 3 - PQ).
**Дано:**
- `y₁₂=1-4j`, `y₁₃=2-5j`, `y₂₃=3-6j`. Шунтів немає.
- "Плаский старт": `U₁=1.02∠0°`, `U₂=1.0∠0°`, `U₃=1.0∠0°`.
- `δ₂=0`, `δ₃=0`, `|U₂|=1.0`, `|U₃|=1.0`.

**1. Розрахунок `∂P₂/∂δ₃` (недіагональний елемент)**
- Формула: `|U₂||U₃|(G₂₃ sin(δ₂-δ₃) - B₂₃ cos(δ₂-δ₃))`
- З Y-матриці, `Y₂₃ = -y₂₃ = -3+6j`. Отже, `G₂₃ = -3`, `B₂₃ = 6`.
- `dP₂/dδ₃ = (1.0)*(1.0) * (-3*sin(0) - 6*cos(0)) = 1 * (0 - 6*1) = -6`

**2. Розрахунок `∂Q₃/∂|U₂|` (недіагональний елемент)**
- Формула: `|U₃|(G₃₂ sin(δ₃-δ₂) - B₃₂ cos(δ₃-δ₂))`
- `Y₃₂ = Y₂₃`, отже `G₃₂ = -3`, `B₃₂ = 6`.
- `dQ₃/d|U₂| = (1.0) * (-3*sin(0) - 6*cos(0)) = 1 * (0 - 6*1) = -6`

## 2. Приклад розрахунку на C#
Код демонструє **формування повної матриці Якобі** для 3-вузлової схеми в заданому стані.

```csharp
// Program.cs
using System;
using System.Numerics;
using System.Collections.Generic;
using MathNet.Numerics.LinearAlgebra;
using MathNet.Numerics.LinearAlgebra.Complex;

public class Program
{
    public static void Main(string[] args)
    {
        // --- Вхідні дані ---
        var y12 = new Complex(1, -4);
        var y13 = new Complex(2, -5);
        var y23 = new Complex(3, -6);

        var Y = Matrix<Complex>.Build.Dense(3, 3);
        Y[0, 0] = y12 + y13;
        Y[1, 1] = y12 + y23;
        Y[2, 2] = y13 + y23;
        Y[0, 1] = Y[1, 0] = -y12;
        Y[0, 2] = Y[2, 0] = -y13;
        Y[1, 2] = Y[2, 1] = -y23;

        var U = Vector<Complex>.Build.DenseOfArray(new[] {
            Complex.FromPolarCoordinates(1.02, 0), // U1 (балансуючий)
            Complex.FromPolarCoordinates(1.0, 0),  // U2 (PQ)
            Complex.FromPolarCoordinates(1.0, 0)   // U3 (PQ)
        });
        
        // Вказуємо індекси вузлів, для яких будуємо Якобіан (PQ-вузли)
        var pqNodeIndices = new List<int> { 1, 2 }; // Вузли 2 та 3 (в 0-індексації)
        var jacobian = BuildJacobian(Y, U, pqNodeIndices);

        Console.WriteLine("Матриця Якобі (4x4):\n" + jacobian.ToMatrixString("F4"));
    }

    /// <summary>
    /// Універсальна функція для побудови матриці Якобі.
    /// </summary>
    /// <param name="yBus">Y-матриця системи.</param>
    /// <param name="voltages">Вектор напруг на поточній ітерації.</param>
    /// <param name="pqNodeIndices">Список індексів PQ-вузлів.</param>
    public static Matrix<double> BuildJacobian(Matrix<Complex> yBus, Vector<Complex> voltages, List<int> pqNodeIndices)
    {
        int n = pqNodeIndices.Count; // Кількість PQ-вузлів, розмір Якобіана буде 2n x 2n
        var jacobian = Matrix<double>.Build.Dense(2 * n, 2 * n);

        // Перед розрахунком похідних нам потрібні P_calc та Q_calc для діагональних елементів.
        // Розрахуємо їх один раз для всіх вузлів.
        var calculatedPowers = Vector<Complex>.Build.Dense(voltages.Count);
        for (int k = 0; k < voltages.Count; k++)
        {
            Complex currentInjection = yBus.Row(k).DotProduct(voltages);
            calculatedPowers[k] = voltages[k] * currentInjection.Conjugate();
        }

        // Цикл по рядках блоків Якобіана (відповідають рівнянням для вузлів i)
        for (int i_idx = 0; i_idx < n; i_idx++)
        {
            // Цикл по стовпцях блоків Якобіана (відповідають невідомим у вузлах j)
            for (int j_idx = 0; j_idx < n; j_idx++)
            {
                int i = pqNodeIndices[i_idx]; // Реальний індекс вузла i
                int j = pqNodeIndices[j_idx]; // Реальний індекс вузла j

                var Gij = yBus[i, j].Real;
                var Bij = yBus[i, j].Imaginary;
                var Vi = voltages[i].Magnitude;
                var Vj = voltages[j].Magnitude;
                var delta_ij = voltages[i].Phase - voltages[j].Phase;

                // --- Заповнення матриці Якобі згідно з формулами ---
                if (i == j) // ДІАГОНАЛЬНІ елементи (i = j)
                {
                    double Pi_calc = calculatedPowers[i].Real;
                    double Qi_calc = calculatedPowers[i].Imaginary;
                    var Gii = yBus[i,i].Real;
                    var Bii = yBus[i,i].Imaginary;
                    
                    // Верхній лівий блок: ∂P/∂δ
                    jacobian[i_idx, j_idx] = -Qi_calc - Bii * Vi*Vi;
                    // Верхній правий блок: ∂P/∂|U|
                    jacobian[i_idx, j_idx + n] = (Pi_calc / Vi) + Gii * Vi;
                    // Нижній лівий блок: ∂Q/∂δ
                    jacobian[i_idx + n, j_idx] = Pi_calc - Gii * Vi*Vi;
                    // Нижній правий блок: ∂Q/∂|U|
                    jacobian[i_idx + n, j_idx + n] = (Qi_calc / Vi) - Bii * Vi;
                }
                else // НЕДІАГОНАЛЬНІ елементи (i ≠ j)
                {
                    // Верхній лівий блок: ∂P/∂δ
                    jacobian[i_idx, j_idx] = Vi * Vj * (Gij * Math.Sin(delta_ij) - Bij * Math.Cos(delta_ij));
                    // Верхній правий блок: ∂P/∂|U|
                    jacobian[i_idx, j_idx + n] = Vi * (Gij * Math.Cos(delta_ij) + Bij * Math.Sin(delta_ij));
                    // Нижній лівий блок: ∂Q/∂δ
                    jacobian[i_idx + n, j_idx] = -Vi * Vj * (Gij * Math.Cos(delta_ij) + Bij * Math.Sin(delta_ij));
                    // Нижній правий блок: ∂Q/∂|U|
                    jacobian[i_idx + n, j_idx + n] = Vi * (Gij * Math.Sin(delta_ij) - Bij * Math.Cos(delta_ij));
                }
            }
        }
        return jacobian;
    }
}
```

### 3. Перевірка правильності обрахунку
1.  **Порівняйте з ручним розрахунком:** Для "плаского старту", порівняйте значення елементів `J[0,1]` (відповідає `∂P₂/∂δ₃`) та `J[3,0]` (відповідає `∂Q₃/∂|U₂|`) у виведеній матриці з вашими ручними розрахунками. Вони мають збігатися.
2.  **Структура:** Переконайтесь, що вивід програми має коректну розмірність 4x4.

## 4. Завдання для самостійної роботи
1. Адаптуйте код для розрахунку матриці Якобі для мережі з 4-х вузлів (1-баланс, 2,3,4-PQ). Якобіан матиме розмірність 6x6.
2. Для вашого варіанту **розрахуйте вручну** для початкового стану (`|U|=1.0`, `δ=0` для вузлів 2,3,4) два елементи якобіана: `∂P₂/∂δ₃` та `∂Q₄/∂|U₂|`.
3. Порівняйте ручний розрахунок з результатом програми для верифікації.

**Схема:** Гілки: 1-2, 1-3, 2-3, 2-4, 3-4.

**Параметри для варіантів (провідності `G-jB`):**

| Вар. | y12 | y13 | y23 | y24 | y34 |
|---|---|---|---|---|---|
| 1 | 1-j4 | 2-j5 | 1-j3 | 0.5-j2 | 2-j4 |
| 2 | 2-j3 | 1-j6 | 1-j2 | 0.6-j2.5 | 1-j5 |
| 3 | 3-j5 | 2-j4 | 2-j4 | 0.4-j1.5 | 2-j6 |
| 4 | 1-j6 | 3-j3 | 2-j5 | 0.7-j3 | 3-j3 |
| 5 | 2-j7 | 1-j2 | 3-j6 | 0.8-j3.5 | 1-j2 |
| 6 | 3-j2 | 2-j1 | 3-j7 | 0.9-j4 | 2-j1 |
| 7 | 1-j5 | 3-j6 | 4-j8 | 1.0-j4.5 | 3-j6 |
| 8 | 2-j4 | 1-j7 | 4-j9 | 0.3-j1 | 1-j7 |
| 9 | 3-j6 | 2-j8 | 3-j8 | 0.5-j2.5 | 2-j8 |
| 10 | 1-j8 | 3-j9 | 2-j7 | 0.6-j3 | 3-j9 |