# Лекція 14: Практичне застосування: Розріджений розрахунок потокорозподілу (DC Power Flow) та модальний аналіз стійкості

## 1. Теоретична частина

### Від фізичних рівнянь поля до задач електроенергетики

У класичній обчислювальній фізиці розріджені матриці найчастіше виникають при дискретизації диференціальних рівнянь у частинних похідних методом скінченних різниць (наприклад, розв'язання стаціонарного рівняння Шредінгера для частинки в потенціальній ямі). 

В електроенергетиці ми стикаємося з безпосереднім аналогом цієї задачі:
1. **Розрахунок потокорозподілу (DC Power Flow):** Закони Кірхгофа та балансу потужностей у лінеаризованому наближенні утворюють систему лінійних алгебраїчних рівнянь, яка математично еквівалентна дискретному рівнянню Пуассона на розрідженому графі енергосистеми.
2. **Модальний аналіз малосигнальної стійкості (Modal Analysis):** Оцінка стійкості паралельної роботи синхронних генераторів та запасів статичної стійкості за напругою зводиться до розв'язання **розрідженої задачі на власні значення** ($A \cdot v = \lambda \cdot v$).

---

### Частина А: Лінеаризований розрахунок потокорозподілу (DC Power Flow)

Повні нелінійні рівняння балансу активної потужності для кожного вузла $i$ мають вигляд:
$$P_i = \sum_{j=1}^N |U_i| |U_j| \big( G_{ij} \cos(\theta_i - \theta_j) + B_{ij} \sin(\theta_i - \theta_j) \big)$$

Для високовольтних магістральних мереж (110–750 кВ) застосовують фізично обґрунтовані інженерні припущення:
1. **Висока індуктивність ліній:** Активний опір ЛЕП набагато менший за реактивний ($R \ll X$), тому провідністю $G_{ij}$ нехтують порівняно з реактивною провідністю лінії $b_{ij} = \frac{1}{x_{ij}}$.
2. **Близькість модулів напруг до номінальних:** Приймають $|U_i| \approx 1.0$ в.о. (в іменованих одиницях або pu).
3. **Малі кути між суміжними вузлами:** Різниця фазових кутів напруги на кінцях ЛЕП невелика ($|\theta_i - \theta_j| < 15^\circ$), звідки $\sin(\theta_i - \theta_j) \approx \theta_i - \theta_j$, а $\cos(\theta_i - \theta_j) \approx 1$.

З урахуванням цих припущень отримуємо елегантну **лінійну розріджену систему (DC Power Flow)**:
$$[P] = [B'] \cdot [\theta]$$
де:
- $[P]$ — вектор заданих вузлових інжекцій активної потужності ($P_i = P_{G,i} - P_{L,i}$);
- $[\theta]$ — шуканий вектор фазових кутів напруги у радіанах;
- $[B']$ — розріджена матриця реактивних провідностей мережі:
  $$B'_{ii} = \sum_{j \in \Omega_i} \frac{1}{x_{ij}}, \qquad B'_{ij} = -\frac{1}{x_{ij}}$$

#### Гранична умова (Балансуючий вузол / Slack Bus)
Оскільки матриця $B'$ сингулярна (сума елементів у кожному рядку дорівнює нулю), електрична мережа вимагає точки відліку фази (аналог нульового потенціалу землі або нульової хвильової функції на межах потенціальної ями). 
Обирається балансуючий вузол (зазвичай вузол 1), кут якого фіксується:
$$\theta_1 = 0$$
Викресливши рядок і стовпець вузла 1, отримуємо редуковану систему з симетричною додатно визначеною розрідженою матрицею $B'_{\text{red}}$ розміром $(N-1) \times (N-1)$:
$$[B'_{\text{red}}] \cdot [\theta_{\text{red}}] = [P_{\text{red}}]$$

Після знаходження кутів перетоки активної потужності по кожній лінії $i-j$ розраховуються миттєво:
$$P_{ij} = \frac{\theta_i - \theta_j}{x_{ij}}$$

---

### Частина Б: Модальний аналіз та критичні власні значення

В енергосистемах надзвичайно важливо знати запас стійкості: наскільки мережа далека від критичного перевантаження або лавини напруги (Voltage Collapse). 
Для цього аналізують спектр власних значень редукованої матриці $B'_{\text{red}}$ або матриці Якобі:
$$B'_{\text{red}} \cdot v = \lambda \cdot v$$

- **Фізичний зміст:** Власні значення $\lambda_k$ характеризують жорсткість електричних зв'язків.
- **Критична мода:** Найменше власне значення $\lambda_{\min}$ є показником **запасу статичної стійкості**. Якщо при зростанні навантаження $\lambda_{\min} \to 0$, система втрачає стійкість (настає колапс напруги). Власний вектор $v_{\min}$, що відповідає $\lambda_{\min}$, вказує на "найслабший вузол" енергосистеми.

> **Нотатка від професора: Чому для власних значень потрібні розріджені методи?**
>
> Для мережі на 5 000 вузлів класичні методи знаходять усі 5 000 власних значень, витрачаючи хвилини і гігабайти пам'яті. Але диспетчеру енергосистеми не потрібні всі 5 000 мод! Його цікавить лише **одне найменше критичне значення $\lambda_{\min}$**. 
> 
> За допомогою розрідженого **степеневого методу зі зсувом (Inverse Power Iteration)** ми знаходимо $\lambda_{\min}$ за лічені мілісекунди, виконуючи на кожній ітерації лише швидке розв'язання розрідженої системи $B'_{\text{red}} w_{k+1} = v_k$.

> **Глибока інтуїція: Механічна аналогія пружних пружин**
> Рівняння $P = B' \cdot \theta$ є точним математичним аналогом закону Гука $F = k \cdot x$ для багатовимірної системи пружин. Фазові кути $\theta$ — це механічні зміщення вузлів (масових точок). Потужності інжекцій $P$ — це зовнішні сили, що тягнуть вузли. Елементи матриці реактивної провідності $b_{ij} = 1/x_{ij}$ — це коефіцієнти жорсткості пружин, що з'єднують вузли.
> Якщо навантаження зростає (сила збільшується), пружини розтягуються — кути $\theta$ стають більшими. Модальний аналіз на власні значення — це пошук "найслабшої ланки" в цій пружній системі. Найменше власне значення $\lambda_{\min}$ показує еквівалентну жорсткість системи вздовж найуразливішого напрямку. Якщо $\lambda_{\min}$ наближається до нуля, це означає, що система втрачає пружність: найменша додаткова сила (потужність) викличе нескінченне розтягування пружин, що фізично відповідає втраті статичної стійкості (колапсу). Власний вектор $v_{\min}$ точно вказує диспетчеру, які саме вузли "поповзуть" першими і де потрібно терміново ввести резервну генерацію.

---

### Приклад ручного розрахунку: 4-вузлова енергосистема

Розглянемо 4-вузлову мережу:
- Вузол 1: Slack (баланс), $P_1$ невідоме, $\theta_1 = 0$.
- Вузол 2: Генератор, $P_{G2} = 1.2$ в.о.
- Вузол 3: Навантаження, $P_{L3} = 1.0$ в.о. ($P_3 = -1.0$).
- Вузол 4: Навантаження, $P_{L4} = 0.8$ в.о. ($P_4 = -0.8$).

Опори ЛЕП:
- Лінія 1-2: $x_{12} = 0.1$ ($b_{12} = 10.0$)
- Лінія 1-3: $x_{13} = 0.2$ ($b_{13} = 5.0$)
- Лінія 2-4: $x_{24} = 0.1$ ($b_{24} = 10.0$)
- Лінія 3-4: $x_{34} = 0.25$ ($b_{34} = 4.0$)

```
   (Slack)                     (Gen)
     (1)────────────────────────(2)
      │      x12 = 0.10          │
      │                          │
 x13  │                          │ x24
=0.20 │                          │ =0.10
      │      x34 = 0.25          │
     (3)────────────────────────(4)
   (Load)                      (Load)
```

#### 1. Формування редукованої матриці $B'_{\text{red}}$ (вузли 2, 3, 4):
Вузол 1 виключено як балансуючий ($\theta_1 = 0$).
- $B'_{22} = b_{12} + b_{24} = 10 + 10 = 20.0$
- $B'_{33} = b_{13} + b_{34} = 5 + 4 = 9.0$
- $B'_{44} = b_{24} + b_{34} = 10 + 4 = 14.0$
- $B'_{23} = B'_{32} = 0$ (немає прямої лінії між вузлами 2 та 3!)
- $B'_{24} = B'_{42} = -b_{24} = -10.0$
- $B'_{34} = B'_{43} = -b_{34} = -4.0$

Матриця $B'_{\text{red}}$ (розмір 3x3):
```
             2       3       4
2  [      20.0     0.0   -10.0 ]
3  [       0.0     9.0    -4.0 ]
4  [     -10.0    -4.0    14.0 ]
```

Вектор правих частин інжекцій потужностей:
$$P_{\text{red}} = [P_2, P_3, P_4]^T = [1.2, -1.0, -0.8]^T$$

#### 2. Розв'язання системи $B'_{\text{red}} \cdot \theta = P$:
Розв'язуємо методом виключення:
1. З першого рівняння: $20 \theta_2 - 10 \theta_4 = 1.2 \implies \theta_2 = 0.06 + 0.5 \theta_4$
2. З другого рівняння: $9 \theta_3 - 4 \theta_4 = -1.0 \implies \theta_3 = -0.1111 + 0.4444 \theta_4$
3. Підставляємо у третє рівняння:
   $$-10(0.06 + 0.5 \theta_4) - 4(-0.1111 + 0.4444 \theta_4) + 14 \theta_4 = -0.8$$
   $$-0.6 - 5 \theta_4 + 0.4444 - 1.7778 \theta_4 + 14 \theta_4 = -0.8$$
   $$7.2222 \theta_4 - 0.1556 = -0.8 \implies 7.2222 \theta_4 = -0.6444$$
   $$\theta_4 = -0.0892 \text{ рад } (-5.11^\circ)$$

Знаходимо інші кути:
- $\theta_2 = 0.06 + 0.5(-0.0892) = 0.0154 \text{ рад } (+0.88^\circ)$
- $\theta_3 = -0.1111 + 0.4444(-0.0892) = -0.1507 \text{ рад } (-8.63^\circ)$
- $\theta_1 = 0.0000 \text{ рад}$

#### 3. Розрахунок перетоків активної потужності:
- $P_{12} = \frac{\theta_1 - \theta_2}{x_{12}} = \frac{0 - 0.0154}{0.1} = -0.154 \text{ в.о.}$ (потужність тече від вузла 2 до 1: $P_{21} = +0.154$)
- $P_{13} = \frac{\theta_1 - \theta_3}{x_{13}} = \frac{0 - (-0.1507)}{0.2} = +0.754 \text{ в.о.}$ (тече від 1 до 3)
- $P_{24} = \frac{\theta_2 - \theta_4}{x_{24}} = \frac{0.0154 - (-0.0892)}{0.1} = \frac{0.1046}{0.1} = +1.046 \text{ в.о.}$
- $P_{34} = \frac{\theta_3 - \theta_4}{x_{34}} = \frac{-0.1507 - (-0.0892)}{0.25} = \frac{-0.0615}{0.25} = -0.246 \text{ в.о.}$ (тече від 4 до 3)

**Баланс на шинах вузла 1 (Slack):**
$$P_1 = P_{12} + P_{13} = -0.154 + 0.754 = +0.600 \text{ в.о.}$$
Перевірка повного балансу потужності системи:
$$\sum P_G = P_1 + P_2 = 0.600 + 1.200 = 1.800 \text{ в.о.}$$
$$\sum P_L = P_{L3} + P_{L4} = 1.000 + 0.800 = 1.800 \text{ в.о.}$$
Баланс зійшовся ідеально: $\sum P_G - \sum P_L = 0$!

---

## 2. Приклад розрахунку на C#

Нижче наведено програму, яка створює розріджену модель 4-вузлової енергосистеми, розв'язує лінеаризований розрахунок потокорозподілу (DC Power Flow) та обчислює мінімальне власне значення стійкості методом розрідженої зворотної ітерації.

```csharp
// Program.cs
using System;
using MathNet.Numerics.LinearAlgebra;
using MathNet.Numerics.LinearAlgebra.Double;

public class Program
{
    public static void Main(string[] args)
    {
        Console.WriteLine("===============================================================");
        Console.WriteLine("   ЛЕКЦІЯ 14: РОЗРІДЖЕНИЙ DC POWER FLOW ТА МОДАЛЬНИЙ АНАЛІЗ   ");
        Console.WriteLine("===============================================================\n");

        int totalBuses = 4;
        int redBuses = totalBuses - 1; // Без балансуючого вузла 1

        // Параметри ліній (From, To, Reactance X)
        // Вузли індексуються: 0 (Вузол 1 - Slack), 1 (Вузол 2), 2 (Вузол 3), 3 (Вузол 4)
        var branches = new (int from, int to, double x)[]
        {
            (0, 1, 0.10),  // 1-2
            (0, 2, 0.20),  // 1-3
            (1, 3, 0.10),  // 2-4
            (2, 3, 0.25)   // 3-4
        };

        // Задані потужності інжекцій (P_gen - P_load)
        double[] P_spec = { 0.0, 1.2, -1.0, -0.8 }; // Для вузла 0 P_spec визначиться з балансу

        // 1. Формування повної розрідженої матриці B_prime
        var B_full = Matrix<double>.Build.Sparse(totalBuses, totalBuses);
        foreach (var (i, j, x) in branches)
        {
            double b = 1.0 / x;
            B_full[i, i] += b;
            B_full[j, j] += b;
            B_full[i, j] -= b;
            B_full[j, i] -= b;
        }

        // 2. Виділення редукованої розрідженої матриці B_red (виключаємо рядок/стовпець 0)
        var B_red = Matrix<double>.Build.Sparse(redBuses, redBuses);
        for (int i = 1; i < totalBuses; i++)
        {
            for (int j = 1; j < totalBuses; j++)
            {
                if (Math.Abs(B_full[i, j]) > 1e-9)
                {
                    B_red[i - 1, j - 1] = B_full[i, j];
                }
            }
        }

        Console.WriteLine("Редукована матриця B'_red (3x3):");
        Console.WriteLine(B_red.ToMatrixString("F2"));

        // Вектор правих частин інжекцій для вузлів 2, 3, 4
        var P_red = Vector<double>.Build.Dense(new double[] { P_spec[1], P_spec[2], P_spec[3] });

        // 3. Розв'язання системи B_red * theta_red = P_red (DC Power Flow)
        var theta_red = B_red.Solve(P_red);

        // Повний вектор кутів (з урахуванням theta_slack = 0)
        var theta_full = new double[totalBuses];
        theta_full[0] = 0.0;
        for (int i = 0; i < redBuses; i++)
        {
            theta_full[i + 1] = theta_red[i];
        }

        Console.WriteLine("--- Результати розрахунку фазових кутів напруги ---");
        for (int i = 0; i < totalBuses; i++)
        {
            double deg = theta_full[i] * 180.0 / Math.PI;
            Console.WriteLine($"Вузол {i + 1}: theta = {theta_full[i],8:F4} рад  ({deg,6:F2} град)");
        }

        // 4. Розрахунок перетоків активної потужності по лініях
        Console.WriteLine("\n--- Перетоки потужності по ЛЕП (P_ij = (theta_i - theta_j) / x_ij) ---");
        double P_slack = 0.0;
        foreach (var (from, to, x) in branches)
        {
            double P_flow = (theta_full[from] - theta_full[to]) / x;
            Console.WriteLine($"Лінія {from + 1} -> {to + 1}: P = {P_flow,7:F4} в.о.");
            
            if (from == 0) P_slack += P_flow;
            if (to == 0) P_slack -= P_flow;
        }

        Console.WriteLine($"\nПотужність балансуючого вузла (Slack Gen P1): {P_slack:F4} в.о.");
        Console.WriteLine($"Сумарний небаланс енергосистеми: {Math.Abs(P_slack + P_spec[1] + P_spec[2] + P_spec[3]):E4}");

        // 5. Модальний аналіз: розрахунок мінімального власного значення lambda_min
        // Метод зворотної ітерації (Inverse Power Iteration)
        var v = Vector<double>.Build.Dense(new double[] { 1.0, 1.0, 1.0 });
        v = v.Normalize(2);

        double lambda_min = 0.0;
        for (int iter = 0; iter < 50; iter++)
        {
            // w = (B_red)^(-1) * v
            var w = B_red.Solve(v);
            double norm = w.Norm(2);
            v = w / norm;
            
            // Відношення Релея: lambda = v^T * B_red * v
            lambda_min = v.DotProduct(B_red * v);
        }

        Console.WriteLine("\n--- Модальний аналіз стійкості (Critical Eigenvalue) ---");
        Console.WriteLine($"Найменше власне значення (lambda_min) : {lambda_min:F4}");
        Console.WriteLine("Критичний власний вектор v_min           : " + v.ToVectorString("F4"));
        Console.WriteLine(">> Запас статичної стійкості підтверджено: lambda_min > 0 (матриця строго додатна).");
    }
}
```

---

## 3. Перевірка правильності обрахунку

1. **Баланс першого закону Кірхгофа (Баланс потужностей):**
   Сума всієї генерації повинна дорівнювати сумі всього споживання:
   $$P_{\text{slack}} + \sum_{i \in \text{Gen}} P_{G,i} - \sum_{j \in \text{Load}} P_{L,j} = 0$$
2. **Вузловий баланс струмів:**
   Для кожного проміжного вузла $i$ алгебраїчна сума перетоків по лініях, що до нього підключені, повинна дорівнювати заданій інжекції $P_i$:
   $$\sum_{j \in \Omega_i} P_{ij} = P_i$$
3. **Коректність власного значення:**
   Для розрахованої власної пари $(\lambda_{\min}, v)$ норма нев'язки власного вектора повинна бути близькою до нуля:
   $$\|B'_{\text{red}} \cdot v - \lambda_{\min} v\|_2 < 10^{-6}$$
   Усі власні значення пасивної мережі повинні бути строго додатними ($\lambda_i > 0$).

---

## 4. Завдання для самостійної роботи

Для параметрів енергосистеми свого варіанту виконайте:
1. Сформуйте розріджену редуковану матрицю реактивних провідностей $B'_{\text{red}}$ (прийнявши вузол 1 за балансуючий).
2. Запишіть вектор вузлових інжекцій активної потужності $P_{\text{red}}$.
3. Розв'яжіть задачу DC Power Flow у середовищі C# та визначте вектори фазових кутів $\theta$ у градусах.
4. Розрахуйте перетоки активної потужності по всіх лініях електропередавання та генеруючу потужність вузла 1 $P_1$.
5. Обчисліть мінімальне власне значення $\lambda_{\min}$ методом зворотної ітерації та вкажіть найслабший вузол мережі.

### Варіанти завдань:

Опори ліній: $x_{12} = 0.10$, $x_{13} = 0.20$, $x_{24} = 0.10$, $x_{34} = 0.25$ в.о. (якщо вказана додаткова лінія 2-3, її опір $x_{23} = 0.15$).

| Вар | Генерація $P_{G2}$ (вузол 2) | Навантаження $P_{L3}$ (вузол 3) | Навантаження $P_{L4}$ (вузол 4) | Наявність лінії 2-3 |
| :---: | :---: | :---: | :---: | :---: |
| **1** | 1.50 в.о. | 1.20 в.о. | 0.90 в.о. | Ні |
| **2** | 1.00 в.о. | 0.80 в.о. | 0.70 в.о. | Так ($x_{23} = 0.15$) |
| **3** | 2.00 в.о. | 1.50 в.о. | 1.10 в.о. | Ні |
| **4** | 0.80 в.о. | 0.90 в.о. | 0.60 в.о. | Так ($x_{23} = 0.15$) |
| **5** | 1.30 в.о. | 1.00 в.о. | 1.00 в.о. | Ні |
| **6** | 1.70 в.о. | 1.40 в.о. | 0.80 в.о. | Так ($x_{23} = 0.15$) |
| **7** | 2.20 в.о. | 1.60 в.о. | 1.20 в.о. | Ні |
| **8** | 0.90 в.о. | 0.70 в.о. | 0.80 в.о. | Так ($x_{23} = 0.15$) |
| **9** | 1.60 в.о. | 1.10 в.о. | 1.20 в.о. | Ні |
| **10**| 1.40 в.о. | 1.30 в.о. | 0.70 в.о. | Так ($x_{23} = 0.15$) |
