# Лекція 6: Методи оптимізації першого порядку (градієнтний спуск)

## 1. Теоретична частина

### Ідея градієнтних методів
У попередній лекції ми навчились ставити задачу оптимізації та розраховувати цільову функцію (втрати). Тепер постає питання: як саме змінювати керовані змінні, щоб знайти мінімум цієї функції?

Методи першого порядку - це ітераційні алгоритми, що використовують інформацію про **перші похідні** цільової функції для знаходження напрямку руху до мінімуму. Вони схожі на людину, що спускається з гори в густому тумані: вона не бачить всієї картини, але може визначити напрямок "найкрутішого схилу" під ногами і зробити крок у цьому напрямку.

### Поняття градієнта
**Градієнт** `∇F` - це вектор, що складається з часткових похідних функції по кожній зі змінних. Простими словами, він збирає в собі інформацію про "крутизну схилу" вздовж кожної координатної осі.

`∇F(X) = [ ∂F/∂x₁, ∂F/∂x₂, ..., ∂F/∂xₙ ]`

**Ключова властивість:** Вектор градієнта `∇F` завжди вказує на напрямок **найшвидшого зростання** функції. Відповідно, вектор **`-∇F` (антиградієнт)** вказує на напрямок **найшвидшого спадання**. Саме в цьому напрямку нам і потрібно рухатись, щоб знайти мінімум.

### Алгоритм градієнтного спуску
Ідея методу полягає в тому, щоб, починаючи з деякої початкової точки, ітераційно рухатись у напрямку антиградієнта. Формула одного кроку виглядає так:

**X⁽ᵏ⁺¹⁾ = X⁽ᵏ⁾ - α * ∇F(X⁽ᵏ⁾)**

де:
- `X⁽ᵏ⁾` - поточне положення (вектор керованих змінних).
- `∇F(X⁽ᵏ⁾)` - градієнт, розрахований у поточній точці.
- `α` - **крок оптимізації (швидкість навчання, learning rate)**. Це скалярний параметр, що визначає, наскільки великий крок ми робимо в обраному напрямку.

> **Нотатка від професора: Про важливість кроку α**
>
> Вибір `α` - це справжнє мистецтво в оптимізації.
> *   **Якщо `α` занадто малий:** Ви будете робити крихітні кроки і спускатися з гори дуже повільно, ризикуючи так і не дійти до мінімуму за розумний час.
> *   **Якщо `α` занадто великий:** Ви можете зробити такий величезний крок, що "перестрибнете" долину і опинитесь на протилежному схилі ще вище, ніж були. Алгоритм стане нестабільним і ніколи не знайде мінімум.
>
> Тому підбір правильного `α` є ключовим для успішної роботи методу.

Алгоритм зупиняється, коли норма градієнта стає дуже близькою до нуля (ми на "пласкій" ділянці, ймовірно, в мінімумі) або після досягнення максимальної кількості ітерацій.

### Приклад ручного розрахунку: Один крок
Знайдемо наступну точку для функції `F(x, y) = (x - 3)² + 2*(y - 5)²`.
- **Початкова точка:** `X₀ = [0, 0]`
- **Крок:** `α = 0.1`

1.  **Часткові похідні (компоненти градієнта):**
    `∂F/∂x = 2*(x - 3)`
    `∂F/∂y = 4*(y - 5)`
2.  **Градієнт в точці `X₀ = [0, 0]`:**
    `∇F(X₀) = [ 2*(0 - 3), 4*(0 - 5) ] = [ -6, -20 ]`
3.  **Робимо крок:**
    `X₁ = X₀ - α * ∇F(X₀) = [0, 0] - 0.1 * [-6, -20] = [0, 0] - [-0.6, -2.0] = [0.6, 2.0]`

**Результат:** Нова точка `X₁ = [0.6, 2.0]`, ближча до мінімуму `(3, 5)`.

## 2. Приклад розрахунку на C#
Реалізуємо повний градієнтний спуск для функції `F(x, y) = (x - 3)² + 2*(y - 5)²`.

```csharp
// Program.cs
using System;
using MathNet.Numerics.LinearAlgebra;

public class Program
{
    // Цільова функція, яку мінімізуємо
    public static double ObjectiveFunction(Vector<double> point)
    {
        double x = point[0];
        double y = point[1];
        return Math.Pow(x - 3, 2) + 2 * Math.Pow(y - 5, 2);
    }

    // Функція для розрахунку градієнта (вектора часткових похідних)
    public static Vector<double> Gradient(Vector<double> point)
    {
        double x = point[0];
        double y = point[1];
        double dfdx = 2 * (x - 3); // ∂F/∂x
        double dfdy = 4 * (y - 5); // ∂F/∂y
        return Vector<double>.Build.Dense(new[] { dfdx, dfdy });
    }

    public static void Main(string[] args)
    {
        // --- Параметри алгоритму ---
        double stepSize = 0.1; // Крок α
        int maxIterations = 100;
        double tolerance = 1e-6; // Точність для зупинки

        // Початкова точка
        var currentPoint = Vector<double>.Build.Dense(new[] { 0.0, 0.0 });

        Console.WriteLine($"--- Градієнтний спуск для F(x,y) = (x-3)² + 2(y-5)² ---");
        Console.WriteLine($"Початкова точка: {currentPoint}");
        Console.WriteLine($"Крок (alpha): {stepSize}\n");

        for (int i = 0; i < maxIterations; i++)
        {
            // 1. Розраховуємо значення функції та градієнт у поточній точці
            double currentValue = ObjectiveFunction(currentPoint);
            var grad = Gradient(currentPoint);
            
            Console.WriteLine($"Ітер. {i:D2}: Точка={currentPoint.ToVectorString("F4")}, F(x,y)={currentValue:F6}, ||grad||={grad.L2Norm():F6}");
            
            // 2. Перевіряємо умову зупинки (чи досягли ми "дна")
            if (grad.L2Norm() < tolerance)
            {
                Console.WriteLine("\nЗбіжність досягнута.");
                break;
            }

            // 3. Робимо крок у напрямку антиградієнта
            currentPoint = currentPoint - stepSize * grad;
            
            if (i == maxIterations - 1) Console.WriteLine("\nПеревищено ліміт ітерацій.");
        }
        
        Console.WriteLine("\n--- Результат ---");
        Console.WriteLine($"Знайдений мінімум у точці: {currentPoint.ToVectorString("F4")}");
        Console.WriteLine($"Значення функції в мінімумі: {ObjectiveFunction(currentPoint):F8}");
    }
}
```

### 3. Перевірка правильності обрахунку
1.  **Перший крок:** Переконайтесь, що після першої ітерації `currentPoint` дорівнює `[0.6, 2.0]`, як у ручному розрахунку.
2.  **Збіжність:** Програма має зійтись до точки, дуже близької до аналітичного мінімуму `(3, 5)`.
3.  **Експеримент з кроком:** Спробуйте змінити `stepSize` на `0.01` (збіжність буде значно повільнішою) та на `0.6` (алгоритм "розійдеться", значення функції почне зростати).

**Висновок:** Градієнтний спуск є надійним і простим для реалізації методом. Однак, він використовує інформацію лише про "напрямок схилу", але не про його "кривизну". Тому для складних функцій (схожих на витягнуті яри) він може робити багато маленьких кроків зигзагом. У наступних лекціях ми розглянемо методи, які враховують і кривизну, що дозволяє знаходити мінімум значно швидше.

## 4. Завдання для самостійної роботи
1.  Реалізуйте алгоритм градієнтного спуску для вашої цільової функції.
2.  **Знайдіть аналітично** часткові похідні `∂F/∂x` та `∂F/∂y` для вашого варіанту.
3.  Внесіть ці формули у функцію `Gradient` вашої програми.
4.  Запустіть програму і знайдіть точку мінімуму.

**Цільові функції для варіантів:**

| Вар | `F(x, y)` | Аналітичний мінімум |
|---|---|---|
| 1 | `2*(x - 1)² + (y - 2)²` | `(1, 2)` |
| 2 | `(x + 2)² + 3*(y - 1)²` | `(-2, 1)` |
| 3 | `1.5*(x - 5)² + 2.5*(y + 1)²` | `(5, -1)` |
| 4 | `4*x² + (y - 10)²` | `(0, 10)` |
| 5 | `(x - 8)² + 5*(y - 3)²` | `(8, 3)` |
| 6 | `2*(x + 4)² + 1.2*(y + 5)²` | `(-4, -5)` |
| 7 | `3.5*(x - 2)² + y²` | `(2, 0)` |
| 8 | `1.8*(x - 6)² + 2.2*(y - 6)²` | `(6, 6)` |
| 9 | `(x + 7)² + 4*(y - 1.5)²` | `(-7, 1.5)` |
| 10| `2.1*x² + 3.1*(y + 0.5)²` | `(0, -0.5)`|
