# Лекція 8: Оптимізація реактивної потужності за допомогою градієнтних методів

## 1. Теоретична частина

### Постановка задачі оптимального потокорозподілу (OPF)
Ми переходимо від абстрактних математичних функцій до реальної інженерної задачі - **оптимального потокорозподілу (Optimal Power Flow - OPF)**. Метою OPF є знаходження такого режиму роботи енергосистеми, який є не просто технічно можливим, а **найкращим** за певним критерієм.

**Мета (Цільова функція):** Найчастіше метою є **мінімізація сумарних втрат активної потужності `ΔP_Σ`** в мережі. Менші втрати = менше спаленого палива = вища ефективність та економія.

**Керовані змінні:** В задачі оптимізації реактивної потужності основними "важелями" є:
-   Реактивна потужність генераторів (`Q_ген`).
-   Напруга на шинах генераторів (`U_ген`).
-   Коефіцієнти трансформації.
Для спрощення ми будемо керувати лише `Q_ген`.

**Обмеження:** Всі обмеження з лекції 5 залишаються в силі: рівняння режиму (рівності), межі по напрузі та потужності (нерівності).

### Алгоритм оптимізації на основі градієнта
Для розв'язання цієї складної задачі ми застосуємо вже знайомий нам градієнтний метод.

**Загальний цикл оптимізації:**
1.  **Початковий розрахунок:** Розрахувати усталений режим (як в лекції 2) для початкових значень `Q_ген`. Виміряти початкові втрати `ΔP_Σ`.
2.  **Розрахунок градієнта:** Розрахувати чутливість `∂ΔP_Σ/∂Q_ген`. Це покаже, як зміняться втрати, якщо трохи змінити `Q_ген`.
3.  **Перевірка збіжності:** Якщо градієнт близький до нуля, значить ми в мінімумі. Зупиняємось.
4.  **Коригування змінних:** Робимо крок у напрямку антиградієнта: `Q_ген_нове = Q_ген_старе - α * (∂ΔP_Σ/∂Q_ген)`.
5.  **Врахування обмежень:** Перевірити, чи нове `Q_ген` не вийшло за межі `Q_min`, `Q_max`. Якщо вийшло - "притиснути" його до відповідної межі.
6.  **ПЕРЕРАХУНОК РЕЖИМУ:** Це **ключовий крок!** Оскільки ми змінили `Q_ген`, вся система змінила свій стан. Потрібно **запустити повний розрахунок усталеного режиму** (метод Ньютона з лекції 2), щоб знайти нові узгоджені напруги та втрати.
7.  **Повторення:** Повернутись до кроку 2.

> **Нотатка від професора: Як знайти градієнт для складної системи?**
>
> Для простої функції `F(x,y)` ми могли знайти похідні аналітично. Але як знайти `∂ΔP_Σ/∂Q_ген` для цілої енергосистеми, яка описується тисячами нелінійних рівнянь?
>
> Тут на допомогу приходить **метод скінченних різниць**. Ідея геніально проста:
> 1. Розрахувати базові втрати `P_loss_base`.
> 2. Трохи "штовхнути" керовану змінну: `Q_ген_нове = Q_ген_старе + dQ`, де `dQ` - дуже мале число (напр. 0.01).
> 3. Розрахувати нові втрати `P_loss_perturbed`.
> 4. Градієнт (чутливість) наближено дорівнює: `(P_loss_perturbed - P_loss_base) / dQ`.
>
> Цей чисельний метод дозволяє знайти градієнт для будь-якої системи, не знаючи її внутрішніх рівнянь. Ми використовуємо її як "чорну скриньку".

### Приклад ручного розрахунку: Один крок оптимізації
**Дано (Варіант 1):**
-   Початкове `Qg2_current = 0.20` о.о.
-   Початкові втрати `P_loss_base = 0.150` о.о.
-   Збурення `dQ = 0.01` о.о.
-   Втрати після збурення `P_loss_perturbed = 0.145` о.о.
-   Крок оптимізації `α = 1.0`
-   Межі: `Qg2_min = -0.1`, `Qg2_max = 0.6` о.о.

**1. Розрахунок градієнта (метод скінченних різниць):**
    `gradient = (P_loss_perturbed - P_loss_base) / dQ = (0.145 - 0.150) / 0.01 = -0.5`
    *Інтерпретація: градієнт від'ємний, отже для зменшення втрат потрібно **збільшувати** Q_g2.*

**2. Розрахунок нового значення `Q_g2` (без обмежень):**
    `Qg2_new_calc = Qg2_current - α * gradient = 0.20 - 1.0 * (-0.5) = 0.70`

**3. Врахування обмежень:**
    `0.70` більше за `Qg2_max = 0.6`. Тому спрацьовує верхнє обмеження.
    `Qg2_final = 0.6`

**Результат:** Рекомендоване нове значення `Q_g2 = 0.60` о.о.

## 2. Приклад розрахунку на C# (одна ітерація оптимізації)
Цей приклад демонструє **ключові кроки однієї ітерації** (кроки 2, 4, 5 з алгоритму). Функція `RunPowerFlow` є "заглушкою", що імітує складний розрахунок усталеного режиму (крок 6).

```csharp
// Program.cs
using System;

public class Program
{
    // Функція-заглушка, що імітує розв'язувач потокорозподілу.
    // В реальності тут був би повний розрахунок режиму (як в Лекції 2),
    // який для заданого Q_g2 повертав би загальні втрати P_loss.
    public static double RunPowerFlow(double q_g2)
    {
        // Імітуємо результати для Варіанту 1
        if (Math.Abs(q_g2 - 0.20) < 1e-4) return 0.150; // Базовий режим
        if (Math.Abs(q_g2 - 0.21) < 1e-4) return 0.145; // Збурений режим
        
        // Імітуємо результати для Варіанту 2
        if (Math.Abs(q_g2 - 0.10) < 1e-4) return 0.120;
        if (Math.Abs(q_g2 - 0.11) < 1e-4) return 0.122;

        return 0; // Для інших значень повертаємо 0
    }
    
    public static void Main(string[] args)
    {
        // --- Початкові дані для Варіанту 1 ---
        double Qg2_current = 0.20;
        double Qg2_min = -0.1;
        double Qg2_max = 0.6;
        double stepSize = 1.0; // Коефіцієнт кроку alpha
        double dQ = 0.01;      // Мале збурення для розрахунку градієнта

        Console.WriteLine($"Початкове Q_g2 = {Qg2_current:F2}");

        // 1. Розраховуємо початкові втрати (імітація)
        double pLoss_base = RunPowerFlow(Qg2_current);
        Console.WriteLine($"Початкові втрати ΔP_Σ = {pLoss_base:F5}");

        // 2. Розраховуємо градієнт методом скінченних різниць
        double Qg2_perturbed = Qg2_current + dQ;
        double pLoss_perturbed = RunPowerFlow(Qg2_perturbed);
        double gradient = (pLoss_perturbed - pLoss_base) / dQ;
        Console.WriteLine($"Розрахована чутливість ∂ΔP_Σ/∂Q_g2 = {gradient:F4}");

        // 3. Робимо крок оптимізації
        double Qg2_new_calc = Qg2_current - stepSize * gradient;
        Console.WriteLine($"Нове значення Q_g2 (до обмежень): {Qg2_new_calc:F4}");

        // 4. Враховуємо обмеження ("затискаємо" значення в діапазоні)
        double Qg2_final = Math.Max(Qg2_min, Math.Min(Qg2_max, Qg2_new_calc));

        if (Qg2_new_calc > Qg2_max) Console.WriteLine("! Спрацювало верхнє обмеження.");
        if (Qg2_new_calc < Qg2_min) Console.WriteLine("! Спрацювало нижнє обмеження.");

        Console.WriteLine($"\n--- РЕЗУЛЬТАТ 1-ї ітерації ---");
        Console.WriteLine($"Рекомендоване нове значення Q_g2 = {Qg2_final:F4}");
    }
}
```

### 3. Перевірка правильності обрахунку
1.  **Порівняйте з ручним розрахунком:** Переконайтесь, що `gradient` та `Qg2_final` збігаються з ручним розрахунком для Варіанту 1.
2.  **Логіка:** Перевірте, що при від'ємному градієнті `Qg2` збільшується, і навпаки. Перевірте, що обмеження застосовуються коректно.

## 4. Завдання для самостійної роботи
Виконайте **ручний розрахунок** першого кроку оптимізації для вашого варіанту.
**Алгоритм:**
1.  Запишіть початкові дані з таблиці.
2.  Обчисліть градієнт: `gradient = (P_loss_perturbed - P_loss_base) / dQ`.
3.  Обчисліть нове значення (без обмежень): `Qg2_new_calc = Qg2_current - stepSize * gradient`.
4.  Застосуйте обмеження, обравши фінальне значення `Qg2_final`.
5.  Запишіть результат.

**Дані для варіантів (всі значення у в.о.):**

| Вар | `Qg2_current` | `P_loss_base` | `dQ` | `P_loss_perturbed` | `stepSize` | `Qg2_min` | `Qg2_max` |
|---|---|---|---|---|---|---|---|
| 1 | 0.20 | 0.150 | 0.01 | 0.145 | 1.0 | -0.1 | 0.6 |
| 2 | 0.10 | 0.120 | 0.01 | 0.122 | 1.5 | 0.0 | 0.5 |
| 3 | 0.30 | 0.180 | 0.01 | 0.176 | 1.0 | -0.2 | 0.4 |
| 4 | -0.05 | 0.200 | 0.01 | 0.203 | 2.0 | -0.2 | 0.3 |
| 5 | 0.25 | 0.130 | 0.01 | 0.124 | 1.2 | 0.1 | 0.5 |
| 6 | 0.40 | 0.190 | 0.01 | 0.187 | 0.8 | 0.0 | 0.4 |
| 7 | 0.15 | 0.110 | 0.01 | 0.111 | 1.5 | -0.1 | 0.3 |
| 8 | 0.00 | 0.160 | 0.01 | 0.158 | 1.0 | -0.3 | 0.3 |
| 9 | 0.35 | 0.170 | 0.01 | 0.165 | 1.1 | 0.1 | 0.4 |
| 10 | -0.10 | 0.220 | 0.01 | 0.224 | 2.0 | -0.1 | 0.2 |
