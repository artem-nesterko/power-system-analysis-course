# Лекція 21: Електромеханічні перехідні процеси (Transient Stability) та метод нечесного Ньютона (VDHN)

## 1. Теоретична частина

### Електромеханічні перехідні процеси (Transient Stability)
Аналіз динамічної (перехідної) стійкості дозволяє з'ясувати, чи збережуть генератори синхронну роботу після значних збурень у системі (наприклад, короткого замикання, відключення лінії чи втрати генерації). Основне рівняння, що описує рух ротора синхронного генератора, називається **рівнянням руху ротора (Swing Equation)**:

$$ \frac{d\delta}{dt} = \omega $$
$$ M \frac{d\omega}{dt} = P_m - P_e - D\omega $$

де:
*   $\delta$ — кут ротора (в радіанах);
*   $\omega$ — відхилення швидкості обертання від синхронної (рад/с);
*   $M$ — постійна інерції генератора;
*   $P_m$ — механічна потужність турбіни (приймаємо постійною);
*   $P_e = P_{max} \sin(\delta)$ — електрична потужність, що видається в мережу;
*   $D$ — коефіцієнт демпфування (часто нехтують або об'єднують із навантаженням).

Оскільки $P_e$ нелінійно залежить від $\delta$, система рівнянь є нелінійною. Для чисельного інтегрування в енергетиці найчастіше використовують **неявний метод трапецій (Implicit Trapezoidal Rule)**, який є абсолютно стійким. 
Для системи $y' = f(y)$, крок інтегрування має вигляд:
$$ y_{n+1} = y_n + \frac{\Delta t}{2} [f(y_n) + f(y_{n+1})] $$

Застосувавши метод трапецій до рівнянь генератора на кроці від $t_n$ до $t_{n+1}$, отримаємо систему нелінійних алгебраїчних рівнянь відносно нових значень $\delta_{n+1}$ та $\omega_{n+1}$. Ці рівняння розв'язуються ітераційним методом Ньютона-Рафсона.

### Метод нечесного Ньютона (Very Dishonest Newton Method - VDHN)
На кожному кроці інтегрування (а крок $\Delta t$ зазвичай дуже малий, $0.01 - 0.02$ с) класичний метод Ньютона вимагає оновлення та факторизації матриці Якобі. Оскільки система рівнянь енергосистеми може налічувати десятки тисяч вузлів, постійна перефакторизація є обчислювально надзвичайно дорогою.

Тут на допомогу приходить **Метод нечесного Ньютона (VDHN)**. Суть методу полягає в тому, що матриця Якобі $J$ обчислюється і факторизується **лише один раз** на початку інтегрування (або на початку кожного кроку, або навіть один раз на кілька кроків), і ця ж обернена (або факторизована) матриця використовується для всіх наступних ітерацій. 

> **Нотатка від професора:**
> Назва "Very Dishonest Newton" (Дуже нечесний Ньютон) виникла як жарт серед математиків, оскільки ми "обманюємо" алгоритм, підсовуючи йому застарілий Якобіан. Якщо ми оновлюємо Якобіан хоча б іноді, це просто "Dishonest Newton". Незважаючи на назву, VDHN є де-факто промисловим стандартом у таких програмах як PSS/E або PowerFactory для симуляції перехідних процесів.

> **Глибока інтуїція: Фізика застарілого Якобіана**
> Чому VDHN взагалі працює? Матриця Якобі відображає чутливість системи (як зміна кутів впливає на зміну потужностей). Оскільки крок інтегрування $\Delta t$ дуже малий ($20$ мілісекунд), кути роторів генераторів через їхню величезну механічну інерцію ($M$) просто фізично не встигають значно змінитися за один крок! Відповідно, чутливість системи (Якобіан) залишається майже незмінною. Ми втрачаємо квадратичну швидкість збіжності Ньютона, але виграємо колосальну кількість часу на уникненні факторизації велетенської матриці.

### Приклад ручного розрахунку
Розглянемо один еквівалентний генератор, підключений до шин нескінченної потужності (SMIB).
*   Постійна інерції $M = 0.1$
*   Механічна потужність $P_m = 1.0$ в.о.
*   Крок інтегрування $\Delta t = 0.02$ с.
*   Початковий стан (перед аварією): $\delta_0 = 30^{\circ} = 0.5236$ рад, $\omega_0 = 0$.

Сталася аварія, через яку максимальна потужність лінії впала з $2.0$ до $P_{max} = 0.5$ в.о. (режим під час КЗ).
Рівняння для розрахунку наступної точки $(\delta_1, \omega_1)$ за методом трапецій:
1. $F_1 = \delta_1 - \delta_0 - \frac{\Delta t}{2}(\omega_0 + \omega_1) = 0$
2. $F_2 = \omega_1 - \omega_0 - \frac{\Delta t}{2M} [ (P_m - P_{max} \sin\delta_0) + (P_m - P_{max} \sin\delta_1) ] = 0$

Підставимо числа ($\frac{\Delta t}{2M} = \frac{0.02}{0.2} = 0.1$):
*   $F_1 = \delta_1 - 0.5236 - 0.01 \omega_1 = 0$
*   $F_2 = \omega_1 - 0.1 [ (1.0 - 0.5 \sin(0.5236)) + 1.0 - 0.5 \sin\delta_1 ] = \omega_1 - 0.175 + 0.05 \sin\delta_1 = 0$

**Крок VDHN:** Знайдемо Якобіан один раз у точці початкового наближення $(\delta^{(0)}=\delta_0, \omega^{(0)}=\omega_0)$:
$$ J_0 = \begin{bmatrix} \frac{\partial F_1}{\partial \delta_1} & \frac{\partial F_1}{\partial \omega_1} \\ \frac{\partial F_2}{\partial \delta_1} & \frac{\partial F_2}{\partial \omega_1} \end{bmatrix} = \begin{bmatrix} 1 & -0.01 \\ 0.05 \cos\delta_0 & 1 \end{bmatrix} = \begin{bmatrix} 1 & -0.01 \\ 0.0433 & 1 \end{bmatrix} $$

Нев'язка на нульовій ітерації $F(\delta_0, \omega_0) = \begin{bmatrix} 0 \\ -0.15 \end{bmatrix}$.
Розв'яжемо систему $J_0 \cdot \Delta x = -F$:
$$ \begin{bmatrix} 1 & -0.01 \\ 0.0433 & 1 \end{bmatrix} \begin{bmatrix} \Delta \delta \\ \Delta \omega \end{bmatrix} = \begin{bmatrix} 0 \\ 0.15 \end{bmatrix} $$
Отримаємо: $\Delta \omega \approx 0.149935$, $\Delta \delta \approx 0.001499$.
Нові значення після однієї ітерації: $\delta_1^{(1)} = 0.525099$, $\omega_1^{(1)} = 0.149935$.
На наступній ітерації нев'язка $F$ стає практично нульовою. Збіжність досягнута без оновлення Якобіана!

## 2. Приклад розрахунку на C#
Нижче наведено код на `MathNet.Numerics`, що демонструє реалізацію методу нечесного Ньютона для першого кроку інтегрування під час КЗ.

```csharp
// Program.cs
using System;
using MathNet.Numerics.LinearAlgebra;

public class Program
{
    public static void Main(string[] args)
    {
        Console.WriteLine("--- Метод нечесного Ньютона (VDHN) ---");
        
        // Початкові умови та параметри
        double delta0 = 0.5235987756; // 30 градусів у радіанах
        double omega0 = 0.0;
        
        // Початкове наближення
        var x = Vector<double>.Build.DenseOfArray(new double[] { delta0, omega0 });
        
        // Якобіан, розрахований ОДИН РАЗ на початку кроку (в точці x0)
        double cos_delta0 = Math.Cos(delta0);
        var J = Matrix<double>.Build.DenseOfArray(new double[,] {
            { 1.0, -0.01 },
            { 0.05 * cos_delta0, 1.0 }
        });
        
        Console.WriteLine("Якобіан (J0):");
        Console.WriteLine(J.ToString("F6"));
        
        // Факторизація Якобіана (або обернення для невеликої розмірності)
        var J_inv = J.Inverse();
        
        Console.WriteLine("Обернений Якобіан (J0^-1):");
        Console.WriteLine(J_inv.ToString("F6"));
        
        // Ітерації VDHN
        for (int iter = 1; iter <= 3; iter++)
        {
            double delta = x[0];
            double omega = x[1];
            
            // Вектор нев'язок F(x)
            double f1 = delta - delta0 - 0.01 * omega;
            double f2 = omega - 0.175 + 0.05 * Math.Sin(delta);
            
            var F = Vector<double>.Build.DenseOfArray(new double[] { f1, f2 });
            
            // Крок Ньютона
            var delta_x = -J_inv * F;
            x = x + delta_x;
            
            Console.WriteLine($"Ітерація {iter}:");
            Console.WriteLine($"  F(x) = [{F[0]:F6}, {F[1]:F6}]");
            Console.WriteLine($"  delta_x = [{delta_x[0]:F6}, {delta_x[1]:F6}]");
            Console.WriteLine($"  x_new = [{x[0]:F6}, {x[1]:F6}]");
            
            // Критерій зупинки
            if (delta_x.L2Norm() < 1e-6)
            {
                Console.WriteLine("Збіжність досягнута!");
                break;
            }
        }
    }
}
```

## 3. Перевірка правильності обрахунку
1. Скомпілюйте і запустіть програму. 
2. Зверніть увагу, що матриця `J` та її обернена форма розраховуються до циклу `for` і не змінюються всередині нього.
3. Процес має зійтися вже на 2-й ітерації (нев'язки $F(x)$ стануть нульовими, а `delta_x` меншим за $10^{-6}$).

## 4. Завдання для самостійної роботи
Запрограмуйте обчислення першого кроку $\Delta t = 0.02$ с за методом VDHN для вашого варіанту параметрів (початкова частота $\omega_0 = 0$). Обов'язково виведіть Якобіан та кількість ітерацій до збіжності з точністю $\varepsilon = 10^{-5}$.

| Вар | $M$ (інерція) | $P_m$ (механічна) | $P_{max}$ (в аварії) | $\delta_0$ (початковий кут, рад) |
|---|---|---|---|---|
| 1  | 0.05 | 0.8 | 0.4 | 0.450 |
| 2  | 0.06 | 0.9 | 0.3 | 0.500 |
| 3  | 0.08 | 1.0 | 0.6 | 0.550 |
| 4  | 0.10 | 1.1 | 0.5 | 0.600 |
| 5  | 0.12 | 0.7 | 0.2 | 0.400 |
| 6  | 0.04 | 0.8 | 0.4 | 0.480 |
| 7  | 0.05 | 0.9 | 0.5 | 0.520 |
| 8  | 0.07 | 1.0 | 0.4 | 0.540 |
| 9  | 0.09 | 1.2 | 0.6 | 0.650 |
| 10 | 0.11 | 0.8 | 0.3 | 0.420 |
