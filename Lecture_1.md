# Лекція 1: Вступ до матричних моделей та формування матриці Y

## 1. Теоретична частина

### Навіщо потрібна Y-матриця?

Уявіть енергосистему як велике місто з районами (це **вузли**) та дорогами між ними (це **гілки** - лінії електропередачі, трансформатори). Щоб зрозуміти, як електроенергія рухається по цій системі, нам потрібна "карта доріг", яка показує, наскільки легко струму перетікати з одного району в інший.

**Матриця вузлових провідностей (Y-матриця)** і є такою "картою". Це фундаментальний інструмент в електроенергетиці, тому що він математично описує всю структуру та параметри мережі в одному компактному об'єкті. Знаючи Y-матрицю та напруги у вузлах, ми можемо розрахувати струми, потужності та, врешті-решт, зрозуміти, як працює вся система. Це перший і найважливіший крок для будь-якого аналізу режимів.

### Правила формування Y-матриці

Y-матриця є квадратною, її розмірність N x N, де N - кількість вузлів у схемі (не враховуючи землю/баланс). Елементи матриці - це комплексні числа, що представляють провідність (величина, обернена до опору).

**Правило 1: Діагональні елементи Y<sub>ii</sub> (власна провідність)**

*   **Що це?** Це сумарна провідність "усіх доріг", що підключені безпосередньо до вузла `i`.
*   **Як розрахувати?** Потрібно просто додати провідності всіх гілок, що сходяться у вузлі `i`, а також провідності, що з'єднують цей вузол із землею (шунти).
*   **Аналогія:** Уявіть, що `Yᵢᵢ` - це загальна "пропускна здатність" транспортного вузла `i`.

**Правило 2: Недіагональні елементи Y<sub>ij</sub> (взаємна провідність)**

*   **Що це?** Це провідність, що безпосередньо з'єднує два різні вузли: `i` та `j`.
*   **Як розрахувати?** Вона дорівнює провідності гілки між вузлами `i` та `j`, взятій **зі знаком мінус**. Якщо прямого зв'язку між вузлами немає, цей елемент дорівнює нулю.
*   **Важливо:** Матриця симетрична, тобто `Yᵢⱼ = Yⱼᵢ`.

### Приклад ручного розрахунку

Розглянемо просту мережу з 3-х вузлів.

![Схема мережі](https://i.imgur.com/k4z5d1w.png)

**Дано:**
-   Провідність гілки 1-2: `y₁₂ = 0.1 - 0.4j`
-   Провідність гілки 1-3: `y₁₃ = 0.2 - 0.5j`
-   Провідність гілки 2-3: `y₂₃ = 0.3 - 0.6j`
-   Провідності на землю (шунти): `y₁₀ = 0.0 - 0.1j`, `y₂₀ = 0.0 - 0.2j`, `y₃₀ = 0.0 - 0.3j`

**Формуємо матрицю Y (3x3):**

1.  **Діагональні елементи (сума всього, що підключено до вузла):**
    -   `Y₁₁ = y₁₂ + y₁₃ + y₁₀ = (0.1 - 0.4j) + (0.2 - 0.5j) + (0.0 - 0.1j) = 0.3 - 1.0j`
    -   `Y₂₂ = y₁₂ + y₂₃ + y₂₀ = (0.1 - 0.4j) + (0.3 - 0.6j) + (0.0 - 0.2j) = 0.4 - 1.2j`
    -   `Y₃₃ = y₁₃ + y₂₃ + y₃₀ = (0.2 - 0.5j) + (0.3 - 0.6j) + (0.0 - 0.3j) = 0.5 - 1.4j`

2.  **Недіагональні елементи (провідність між вузлами зі знаком мінус):**
    -   `Y₁₂ = Y₂₁ = -y₁₂ = -(0.1 - 0.4j) = -0.1 + 0.4j`
    -   `Y₁₃ = Y₃₁ = -y₁₃ = -(0.2 - 0.5j) = -0.2 + 0.5j`
    -   `Y₂₃ = Y₃₂ = -y₂₃ = -(0.3 - 0.6j) = -0.3 + 0.6j`

**Результат:**
```
      |  0.3 - 1.0j    -0.1 + 0.4j    -0.2 + 0.5j |
Y  =  | -0.1 + 0.4j     0.4 - 1.2j    -0.3 + 0.6j |
      | -0.2 + 0.5j    -0.3 + 0.6j     0.5 - 1.4j |
```

## 2. Налаштування середовища для C#

Для роботи з прикладами коду вам знадобиться встановити .NET SDK та редактор коду, наприклад, Visual Studio Code.

### Крок 1: Встановлення .NET SDK
Завантажте та встановіть останню версію .NET SDK з офіційного сайту Microsoft: [https://dotnet.microsoft.com/download](https://dotnet.microsoft.com/download)
Після встановлення, відкрийте термінал (командний рядок) та перевірте версію, виконавши команду: `dotnet --version`

### Крок 2: Створення та налаштування проекту
1.  Створіть нову папку для проекту, відкрийте в ній термінал.
2.  Створіть новий консольний додаток: `dotnet new console -n Lecture1_Task`
3.  Перейдіть до папки проекту: `cd Lecture1_Task`
4.  Додайте бібліотеку для роботи з матрицями: `dotnet add package MathNet.Numerics`

### Крок 3: Запуск коду
1.  Відкрийте файл `Program.cs`.
2.  Замініть його вміст кодом з прикладу нижче.
3.  Поверніться до терміналу та виконайте команду: `dotnet run`
У консолі ви побачите результат роботи програми - сформовану Y-матрицю.

## 3. Приклад розрахунку на C#

Програмно сформуємо Y-матрицю для схеми з ручного розрахунку.

```csharp
// Program.cs
using System;
using System.Numerics; // Простір імен для роботи з комплексними числами
using MathNet.Numerics.LinearAlgebra; // Базовий простір імен бібліотеки Math.NET
using MathNet.Numerics.LinearAlgebra.Complex; // Розширення для роботи з комплексними матрицями

public class Program
{
    public static void Main(string[] args)
    {
        // --- 1. Вхідні дані ---
        // Кількість вузлів у мережі (не враховуючи землю)
        int numberOfNodes = 3;

        // Оголошуємо провідності гілок як комплексні числа (real, imaginary)
        var y12 = new Complex(0.1, -0.4);
        var y13 = new Complex(0.2, -0.5);
        var y23 = new Complex(0.3, -0.6);
        
        // Оголошуємо провідності на землю (шунти)
        var y10 = new Complex(0.0, -0.1);
        var y20 = new Complex(0.0, -0.2);
        var y30 = new Complex(0.0, -0.3);

        // --- 2. Ініціалізація матриці ---
        // Створюємо порожню матрицю 3x3, заповнену нулями
        var YMatrix = Matrix<Complex>.Build.Dense(numberOfNodes, numberOfNodes);

        // --- 3. Формування Y-матриці за правилами ---
        // Діагональні елементи (Yii = сума всіх провідностей, підключених до вузла i)
        YMatrix[0, 0] = y12 + y13 + y10; // Вузол 1 (індекс 0)
        YMatrix[1, 1] = y12 + y23 + y20; // Вузол 2 (індекс 1)
        YMatrix[2, 2] = y13 + y23 + y30; // Вузол 3 (індекс 2)

        // Недіагональні елементи (Yij = -yij)
        YMatrix[0, 1] = -y12;
        YMatrix[1, 0] = -y12; // Матриця симетрична

        YMatrix[0, 2] = -y13;
        YMatrix[2, 0] = -y13;

        YMatrix[1, 2] = -y23;
        YMatrix[2, 1] = -y23;

        // --- 4. Виведення результату ---
        Console.WriteLine("Матриця вузлових провідностей (Y-матриця):");
        // Використовуємо спеціальний метод для красивого форматованого виводу
        Console.WriteLine(YMatrix.ToMatrixString("F3")); 
    }
}
```

### 4. Перевірка правильності обрахунку
1.  **Порівняйте результат:** Вивід програми для Y-матриці має точно збігатися з матрицею, отриманою в результаті ручного розрахунку.
2.  **Перевірте знаки:** Особливу увагу зверніть на знаки недіагональних елементів – вони мають бути протилежними до знаків провідностей відповідних гілок.
3.  **Перевірте симетрію:** Матриця Y для пасивних мереж завжди симетрична (`Yᵢⱼ = Yⱼᵢ`). Переконайтесь, що ваша програмна реалізація це забезпечує.

## 5. Завдання для самостійної роботи

На основі наведеного прикладу, створіть консольну програму, яка формує та виводить на екран Y-матрицю для вашого варіанту.

**Схема для всіх варіантів:** 4 вузли.
- Гілки: 1-2, 1-3, 2-3, 2-4, 3-4.
- У вузлі 1 є провідність на землю `y10`.
- У вузлі 4 є провідність на землю `y40`.

**Параметри провідностей (у форматі `G - jB`):**

| Варіант | y12 | y13 | y23 | y24 | y34 | y10 | y40 |
|---|---|---|---|---|---|---|---|
| 1 | 0.1-j0.4 | 0.2-j0.5 | 0.3-j0.6 | 0.1-j0.3 | 0.2-j0.4 | 0-j0.1 | 0-j0.2 |
| 2 | 0.2-j0.3 | 0.1-j0.6 | 0.4-j0.5 | 0.2-j0.2 | 0.1-j0.5 | 0-j0.2 | 0-j0.1 |
| 3 | 0.3-j0.5 | 0.2-j0.4 | 0.1-j0.7 | 0.3-j0.1 | 0.2-j0.6 | 0-j0.3 | 0-j0.3 |
| 4 | 0.1-j0.6 | 0.3-j0.3 | 0.2-j0.8 | 0.1-j0.4 | 0.3-j0.3 | 0-j0.1 | 0-j0.4 |
| 5 | 0.2-j0.7 | 0.1-j0.2 | 0.3-j0.9 | 0.2-j0.5 | 0.1-j0.2 | 0-j0.2 | 0-j0.5 |
| 6 | 0.3-j0.2 | 0.2-j0.1 | 0.4-j0.4 | 0.3-j0.6 | 0.2-j0.1 | 0-j0.3 | 0-j0.6 |
| 7 | 0.1-j0.5 | 0.3-j0.6 | 0.1-j0.3 | 0.1-j0.7 | 0.3-j0.8 | 0-j0.4 | 0-j0.1 |
| 8 | 0.2-j0.4 | 0.1-j0.7 | 0.2-j0.2 | 0.2-j0.8 | 0.1-j0.9 | 0-j0.5 | 0-j0.2 |
| 9 | 0.3-j0.6 | 0.2-j0.8 | 0.3-j0.1 | 0.3-j0.9 | 0.2-j0.7 | 0-j0.6 | 0-j0.3 |
| 10 | 0.1-j0.8 | 0.3-j0.9 | 0.4-j0.2 | 0.1-j0.6 | 0.3-j0.5 | 0-j0.7 | 0-j0.4 |